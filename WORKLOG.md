# Worklog

每次改动的记录,最新在最上面。格式:`- 改了什么(为什么)` — 末尾可带 commit 短哈希;未提交标注「(未提交)」。

- 建立统一图片编辑源：单图也以一个根图层表达，项目素材详情接入三栏分层编辑器、Agent 拆分建议、显隐/文字/层序编辑和新版本保存；资产中心新增活动 KV、直播间与兴趣卡 3 套分层模板，架构基线明确模板/Style Bible/路由/质检的边界与精确版本迁移规则（未提交）
- 统一 AI 工坊平台级导航为互斥页面状态，修复资产中心与项目节点双选、`?page=assets` 同时渲染首页、离开深链后误回首页、项目页无法关闭资产中心；活动玩法配置进入资产中心时记录来源并提供明确返回，选择资产会回到玩法页并写入对话上下文（未提交）
- 将资产中心补成可落地的资产生命周期：新增完整详情页、按资产类型继承模板的新建/变体流程、已发布资产创建草稿版本、可编辑参数组与交付物、文件导入和发布前业务检查（未提交）
- 将资产中心从 9 个泛化样板扩为 17 个可生产资产：吸收心仔 v1.11、热点 Banner v6.5 和官号直播间 v3.40 的真实沉淀，新增 Brand Kit / IP 角色 / 两套 Banner / 直播间分层套件 / 直播权益组件 / 4 套 Style Bible / 4 类字体与 3 个玩法包；每项展示分类型参数、可变权限、交付物、导入格式、来源证据、授权和质检门槛，并补充资产包统一契约（未提交）
- 冻结“资产驱动的活动产品编译器”架构基线并完成抽奖 P0 纵切：新增独立资产中心（Brand Kit / Style / 玩法 / 字体）、Lottery GameplayPackage + Editor Recipe 编译、白名单 Semantic Patch、revision/人工接管持久化、Agent 批次保护与旧错误可渐进修复；项目页展示玩法/品牌/风格/字体/知识 5 项生成依据并分别跳转资产中心或资源库，生成回放会重放人工接管值而不静默覆盖（未提交）
- 撤回玩法配置首屏的共用组合流程图（说明逻辑不能替代成熟配置产品的信息架构）：抽奖页改按抽取内容→出奖规则→参与规则→风险控制组织；集卡页按卡册内容→兑换设置→赠送与反馈组织；任务、投票、答题也分别按自身运营决策顺序重排，跨玩法关系只保留在具体字段与对象绑定中（未提交）
- 玩法配置首屏新增组合玩法总图：把任务、抽卡、卡册和兑奖串成可点击的真实用户链路，明确抽奖产出是卡片而非优惠券；同步展示 9 张卡的基础权重占比、88/12 新卡/重复分池规则、6 抽保底、2/4/7 集卡门槛与三档真实奖品，并提示奖品库中未接入主链路的对象（未提交）
- 重构玩法配置的信息呈现顺序：首屏新增当前生效规则链和可调整项，所有规则组默认摘要展示并按需展开；卡池、奖品、任务改为对象列表且同一时间只编辑一项，避免用户进入页面就被完整参数表淹没（未提交）
- 活动玩法配置从平铺参数表收敛为真实活动实例工作台：首屏明确阶段/版本/状态和模块关键数，参与范围按需展开；技术 ID 改成业务选项，卡片组合改为可选内容，素材按用途筛选并展示中文名；新增活动期库存覆盖/缺口和任务单用户机会上限的实时计算（未提交）
- 活动玩法配置移除「共享能力接入」和常驻发布检查（技术服务 ID 与开发验收不应暴露给运营），改为生效时间、开放平台、入口、人群、地域、资格、参与人认定和风险处理等业务规则；玩法切换、内容背景、标题和面板层级同时对齐项目文档/素材库的轻量工作区风格（未提交）
- 深化活动玩法配置为真实活动对象模型：卡池/候选/题库支持 CSV/JSON 按 ID 合并导入，奖品拆为独立库存与履约对象，卡片/奖品/任务绑定素材 key，补齐任务事件、概率/保底、频控、玩法文案和发布检查，并可直达素材库（未提交）
- 将「活动玩法配置」从方案报表重构为生成后的通用玩法编辑器：一级按抽奖/集卡/任务/投票/答题组织，二级用概率、保底、奖池权重、兑换档位、任务发放等可交互面板调参；奖池权重已接入真实抽卡逻辑，补充玩法架构说明（未提交）
- 依据团队 Design System 重构「活动玩法配置」：移除自造 BI 卡片页、重复标题和橙色流程块，复用 ProductToolbar、860px 对象页 Shell、单层表格/列表与语义状态标签，使玩法总览、创建过程、参数配置、运行管理与项目其他页面统一（未提交）
- 将「活动玩法配置」升级为四页工作台：基于暑期活动方案补齐双轨玩法总览、策划到发布的创建链路、8 组完整配置域、环境版本/发布门禁/监控管理，并保留当前夜食 Demo 可编辑配置；新增玩法拆解说明和装备数量、奖励占位、外部依赖校验（未提交）
- 小马 3D 工作台改为左右布局：预览在左按 4:5 居中并随面板伸缩，参数与换装收进右侧 268px 独立滚动栏；预览不再定高，补 ResizeObserver 让渲染缓冲跟着盒子走（原来只听 window.resize，栏宽变化会拉伸画面）（未提交）
- 素材生成态版式对齐素材库：改用同一套顶栏、`space-y-7 px-5 py-5` 分组与 120px 方形卡片栅格（原来是 760px 居中容器 + 148px 定高 96px 缩略图，同一个东西两套间距）（未提交）
- 编辑面板的操作反馈改用标准 sonner toast：XiahuaEditPanel 去掉自绘的黑底胶囊提示，SummerSurfEditPanel 的「已应用到活动页 / 已恢复默认配置」不再占用常驻状态位（点发布后「配置完整」被永久顶替）（未提交）
- 回放进编辑态时收起右侧「页面框架 / 替换清单」产物栏：画布已把 5 个页面铺开，这条栏说的是同一件事，还挤掉编辑面板和画布的宽度（未提交）
- 对齐活动生成过程与左侧节点：「数据库」改为从当前玩法配置实时推导（卡池/档位/任务/抽卡参数四张表，新增 draw_tasks 与 draw_rules），不再是提前写死的上线终态；收档位那步同时改玩法与项目文档（BuildStep.mutate 支持数组），文档不再停在 4 档；切走「已上线」时按 owner 暂存整场回放，切回半成品项目原样接着搭；夏日冲浪不再同时出现在 createdProjects 和固定列表里（侧栏重复 + React key 冲突）（未提交）
- 修复活动生成链路：素材阶段补回「换素材」卡点（这条支路原本无人跳转、永远播不到），该支路改为切换真有第二版素材的「集卡面板底」（原先指向没有 variants 的任务区，对话说「已切到第二版」但界面切不过去）；卡点选项 key 改用下标（多个选项共用同一 to 会撞 key）；首页附件路由不再把系统兜底文案计入关键词判断（否则空文案上传任何文件都落进活动脚本）；三个入口统一走 resetXiahuaReplay 归零回放状态；补清单分支与模板说明的素材数量统一为 26 项；新增开发期脚本自检，跳转断链与死分支直接 console.warn（未提交）
- 兴趣卡 Logo 改用 SVG 蒙版继承工具条颜色：默认与其他入口一致为黑色，选中后统一为首页选中蓝（未提交）
- AI 工坊首页「兴趣卡模板」改用用户提供的蓝色兴趣卡 SVG；选中后移除玩法、比例、模板、互动四个参数，仅保留可进入资源库的「扩展」按钮，并清理提交中的隐藏默认参数（未提交）
- 将 AI 工坊首页营销活动的模板默认项由「不使用模板」改为「选择模板」，并保持占位项不进入提交指令（未提交）
- 移除 AI 工坊首页选择「营销活动」后的「集卡兑奖」玩法下拉，并将活动形态收敛为「H5 活动页 / 原生化活动页」两项（未提交）
- 将导航宽度缓存与独立工坊切换改为 effect 下一帧同步，消除渲染期 ref 写入和同步 setState 的 ESLint 阻断（未提交）
- 将「夯爆了」上线文档与模板复刻项目文档统一到真实生成产物：同步奖励、抽卡、任务、页面/浮层及金豆未接入边界，并让复刻全阶段保持同一份文档（未提交）
- 将 vendored Three.js 源码排除出项目 ESLint 扫描，避免第三方规则注释与类型声明阻断 CI（未提交）
- 移除 AI 工坊首页「营销活动」参数栏最右侧的活动周期下拉，精简为玩法、活动形态和模板（未提交）
- 将 AI 工坊首页快捷入口「兴趣卡」更名为「兴趣卡模板」，与下方案例分类命名保持一致（未提交）
- 让「营销活动」的模板下拉首次打开就展示「夯爆了 · 集卡 H5 模板」，无需先执行存为模板（未提交）
- 修复从平台首页首次点击项目「素材库」只切回项目、需要再点一次的问题，现在单次点击直接打开素材库（未提交）
- 将小马 3D 工作台统一为黑白灰视觉，并参考活动素材生成海滩、小鸟运动、黑白街头三套可切换换装预览（未提交）
- 移除活动页编辑栏中的小马 3D 工作台入口卡及主视觉内重复入口，仅保留从素材库的小马 3D 对象进入（未提交）
- 将单类型素材库顶部「图像」改为「所有素材」，并让「IP 形象 / 小马 3D 对象」点击后直接打开同一套真实 GLB 小马工作台（未提交）
- 将用户放入项目的 GLB 小马模型接入「小马 IP · 3D 对象工作台」，支持真实材质预览、拖拽视角、灯光/背景控制与 PNG 导出，加载失败时回退旧预览图（未提交）
- 将「更多精彩活动」Banner 从美食主题替换为玩水、露营、音乐、拍照与城市探索主题，避免与主活动夜宵内容重复（未提交）
- 为「这夏夯爆了」底部「更多精彩活动」空白卡生成并接入夜市美食活动图，填充原有 Banner 占位，同时保留线框态与空白模板行为（未提交）
- 侧栏固定展示「夏日冲浪 · 顺风顺水」，并在左侧挂上模板复刻的静态 mock 对话流，支持点击「再放一次」进入真实回放（未提交）
- 导航方案菜单移除选项前的序号，只保留方案名称展示（未提交）
- 导航方案切换仅保留方案 1、4、7，并将方案 4 改名为「文案 Header」（未提交）
- 顶部存模板成功提示覆盖 Sonner 默认固定宽度，改为按内容收缩（max-content），仅在超出视口时限制并省略（未提交）
- 模板复刻改为分批落地：第 1 批完成后先更新夏日主视觉，第 2 批再更新装备卡面，第 3 批补齐奖励、任务和副玩法，未完成部分保留占位（未提交）
- 模板复刻首个产物改为基于用户输入与已生成模板的「夏日冲浪 · 顺风顺水」项目文档，去掉右侧布局等 mock 逻辑描述，也不再重复模拟用户补充需求（未提交）
- 存为模板的成功提示改为单行显示，超出视口时用省略号收尾，避免提示气泡换行变高（未提交）
- 选择核心玩法后增加确认后的思考过渡：gp-1/gp-2 思考期间继续停留在灰阶页面框架，完成回复后才接入可试玩玩法并切换「活动玩法配置」（未提交）
- 重写「选择核心玩法」承接文案：明确页面框架已完成、选择卡在对话下方、右侧仍保留灰阶框架，选定后才生成活动玩法配置，去掉与实际 UI 不符的“右侧列链路和代价”（未提交）
- 回放产物改为完成态切换：所有 think 步骤先保留上一阶段产物，直到对应完成回复出现后才打开/切换新产物，避免刚开始思考右侧就提前出现（未提交）
- 选择核心玩法时继续停留在页面框架预览，用户选定具体玩法后才切到「活动玩法配置」tab，展示同一份可编辑玩法配置（未提交）
- 素材全部生成后预览继续保持灰阶页面框架，直到「生成 UI 界面」才解锁成品；编辑态的移动、隐藏、插入、次数/阶段与玩法修改自动持久化，退出编辑时明确保存（未提交）
- 页面框架确认后的过渡帧直接锁定灰阶线框，补齐跳转步骤的 phase/stage 兜底，消除红底成品预览闪屏（未提交）
- 夏日冲浪素材库接入 Marketing King 原始 Hero 视频缓存 ID：若浏览器缓存存在则自动挂载视频，否则保留真实首尾帧作为海报并明确待导出状态（未提交）
- 夏日冲浪 Hero 配置补回原始 Marketing King 的两个 video-native 资源 ID，图片首尾帧继续作为本地可用兜底（未提交）

- 夏日冲浪项目左侧模块改为真实活动结构：素材库接入 Marketing King 现有素材，玩法配置、数据库、项目文件与真实页面/装备/奖励/任务对齐（未提交）
- 模板确认卡点点击后直接发送确认消息：取消残留的输入框打字计时并清空输入框，不再把确认文案再打一遍（未提交）
- 替换清单的素材项左侧补上夯爆了模板原始素材缩略图与名称，右侧对应新活动替换内容，方便逐项对照（未提交）
- 替换清单隐藏批次编号与“分几批执行”提示，保留顺序替换和逐段确认逻辑（未提交）
- 替换清单右侧接入夏日冲浪活动的真实素材：KV、主角、装备卡、奖励、话题和品牌图直接取现有活动资源，标题与按钮使用活动实际文案（未提交）
- 模板文档改为「模板页面框架」：一次展示主会场、开卡结算、卡册、兑奖和规则 5 个页面，并标注 1–8 号素材替换位；复刻中的模板参考预览保留灰阶线框（未提交）
- 首页参数下拉的模板名称强制单行显示，避免「夯爆了 · 集卡 H5 模板」在选项里断行（未提交）
- 将「夯爆了」抽成可复用的「夯爆了 · 集卡 H5 模板」并新增模板使用说明文档：引用模板后先确认固定页面/玩法骨架与可替换槽位，再按 3 批替换生成夏日冲浪；复刻未完成前只展示模板参考，不再直接露出夏日冲浪成品（未提交）
- 生成阶段素材板移除「再出一版/本地上传」操作，并让已生成素材点击进入与素材库一致的详情视图（未提交）
- 生成流程点击卡点后自动切到对应产物视图：页面框架/玩法/成品→预览，素材清单/素材→素材库，方案→项目文档；缺少的 tab 会自动创建（未提交）
- 导航 7 左侧工作台目录整体上移 8px，缩小顶部留白（未提交）
- 默认导航方案切换为导航 7「内部抖音 AI 工作台」，并通过存储迁移让已有预览一次性切换（未提交）
- 已上线那版活动改名「夯爆了 已上线」（原「这夏夯爆了 · 已上线」，去掉中间的点）：VibeCodingPage 常量连同 project-kinds / project-docs / chat-suggestions / ProjectObjectViews 五处 key 一起改。浏览器实测（未提交）
- 首页发起的搭建/复刻不再把原话回打进输入框再发送：startXiahuaBuild / startTemplateClone 加 `instant` 开关，submitFromHome 传 true 直接开播（回放入口仍保留打字动画）。浏览器实测（未提交）
- 上传文档的附件卡去掉「首页上传 · 已接入活动搭建」那行来源说明：DocAttachment 的 meta 为空时不渲染，首页上传场景传空串。浏览器实测（未提交）
- AI 工坊首页输入框传附件后不再撑高：ChatComposer 高度固定 166（原来附件态跳到 224），附件卡直接挤输入区（textarea 约剩 30px，仍可输入与滚动）。浏览器实测（未提交）
- 建议回复改成并列的下一步：① 第一个不再带勾图标 —— 带了像「确认」，但它并不比后面那个更对。② 去掉「修改方案」「调整页面结构」这类回去改上一步的选项，对应的分支步骤一并删掉；留下的都是具体动作（调整抽卡参数 / 补充清单项 / 重出风格参考 / 替换部分素材）。③ 流程不再只有一条直线：方案这步给两条并列路径 —— 「生成页面框架」按顺序走，或「先定核心玩法」跳着走（选完玩法直接把框架和玩法一起搭出来，少一轮来回，新增 gp-early → 选择卡 → wf-e1/wf-e2/wf-e-done 一条支线）（未提交）
- 修素材库跟阶段脱节：UI 都生成完了素材库还停在「全部待生成」—— 原来只在 assets 阶段按批次算，其它阶段一律传 -1。改成一个派生值 xiahuaAssetDone：清单阶段 -1（一批没开始）、生成阶段跟着批次、生成完与非回放态就是全部完成，素材库自己不再判断阶段，只按这个值刷新（未提交）
- 整理完素材清单直接开出「素材库」tab 并切过去（顶部多一个 tab），产物不再挤在预览位上：预览那一栏在清单与素材生成阶段回到手机本身，素材相关的都归素材库这个 tab。顺带修两个对不上的地方：① 清单阶段预览会提前变成成品图 —— 这时一张素材都还没生成，wireframe 判定补上 assetList，保持灰框；② 素材库计数在「一批都没开始」时显示 22/26 —— done 传 -1 时 slice(0,-1) 被当成「除最后一批都好了」，加了下限夹取，现在是 0/26、六组全部排队中（未提交）
- 对话流五处对齐：① 选择卡最后一行直接就是输入框（不再是"点开才展开"的选项），写完回车或点发送即可。② 修掉选完分支会顺着往下串的 bug —— gp-pick-collect 原来排在其它分支前面，选「集卡兑奖」后播放器按数组顺序又走进了自定义那条、把"我自己描述一套玩法"也发了出去；分支步骤重排成「末尾是卡点的分支在前、集卡分支紧贴 gp-1」。③ 玩法不再在右侧单独出面板（删掉 XiahuaGameplayPanel），这一步右侧就是能试玩的灰框本身。④ 素材清单直接用素材库那个页面视图（删掉 XiahuaAssetManifest），清单和素材库本来就是同一份东西的两个时刻；清单阶段 done=-1，六组全是「排队中」，不会有哪一批假装在生成。⑤ 对话里的产物卡片可点，各自跳到自己的视图：方案→项目文档（回放中就是当前预览位）、清单/素材→素材库、框架/玩法/成品→预览。顺带把思考与回复的措辞跟实际过程对齐（接玩法那段点名"按刚定的集卡兑奖接"、清单那段说清是建进素材库、不再指着已经撤掉的右侧面板说话）（未提交）
- 选核心玩法改成 Claude Code 那种选择卡：一张卡里 1/2/3 三个候选 + 第 4 项「都不是，我自己说」可以直接写，选中或写下的那句话作为一条用户消息发出去（BuildGate.choices 加 input/placeholder，onGate 带 text，按目标步骤 id 覆盖该步的用户文案）。自定义那条会得到「你这套本质还是累积兑换、跟集卡同构，先按集卡接上再改」的回应再回主线。另修一个对不上过程的 bug：回放中点左侧目录的「素材库」会直接看到全部成品图 —— 现在回放期间素材库改用同一份带进度的素材板渲染，清单刚对完是 0/26 全部排队中，跟着批次一批批变成已生成（未提交）
- 方案与素材清单两步改回整屏产物：这两步本来就没有页面可看，右侧不再摆一台空手机 + 侧栏，而是整屏就是产物本身，跟左侧目录的「项目文档 / 素材库」是同一种视图（xiahuaFullArtifact 控制，同时不出编辑按钮和缩放条）。页面框架、核心玩法、素材生成、替换清单这些跟画面相关的阶段仍是「预览常驻 + 右侧产物栏」（未提交）
- 把 0→1 与模板复刻的右侧从「整块换产物」改成「预览常驻 + 产物栏」：工具条下面固定成两栏，左边一直是预览（手机/画板），右边是一条 336px 的产物栏（min 280 / max 70%，窄面板下不至于把手机挤没）。方案、页面框架、核心玩法、素材清单、素材生成、替换清单全部挂进这条栏里，预览不再时有时无、也不会因为产物出现而左右跳。顺带删掉 previewSurface 里残留的那份产物分支（否则素材板会在预览区里再渲一份）。新增两个面板：XiahuaFramePanel（框架阶段列 5 个页面各装了什么）和 XiahuaGameplayPanel（三个候选玩法 + 接上后的链路与参数）。接玩法那步原来那张浮在预览旁边的卡片撤掉了 —— 它会把手机挤走。② 接入玩法改成先选后接：新增选择题式卡点（BuildGate.choices），对话里出三张卡片（集卡兑奖 / 接金豆小游戏 / 投稿任务体系），每张写清楚各自的链路、页面数和代价，选金豆或投稿会得到「为什么建议主玩法还是集卡」的解释再回到主线；接上后右侧面板高亮已选的那套。③ 生成全部素材与预览联动：素材批次映射到预览的搭建层级（主视觉→kv、交互组件→actions、卡面→cardArt、下半屏→sections），右边一批批出图的同时左边手机一层层长出来。④ 引用模板生成新活动也先确认需求：加了一轮「集什么、要几种 / 奖励怎么给 / 要不要副玩法 / 视觉方向」的对齐，再去读模板出替换清单（未提交）

- AI 工坊首页「兴趣卡模板」这一栏换成案例墙：新增 InterestCardShowcase（设计稿 创中 AI创作 3477-14625「看看大家的兴趣卡」），玻璃卡片 = 分类 Tag + 标题 + 说明 + 探出的手机（截图 + phone-frame 外壳，下沿被卡片裁掉），点卡片带着「参考「X」这张兴趣卡做同款」进输入框；卡牌网格只留给其他分类。素材复用 public/assets/workshop 里已有的 phone-frame / phone-ui-answers / words / outfit / dog，另补一张 phone-ui-color.webp（388×843，20KB，TinyPNG 转 webp）。浏览器实测五张一排（未提交）
- 按反馈修八处「时序与呈现」问题：① 玩法配置里的卡面不再提前出现 —— 新增 assetsReady，回放没真的跑到素材阶段（assetBatch ≥ 5）之前卡池一律显示空槽，缩略图同时从行首挪到行尾。② 接入核心玩法这一步右侧原来看不出变化（还是灰框），加了一张贴在预览旁边的「玩法已接入」链路卡：点抽卡扣 1 次 → 出卡 → 点亮卡槽 → 集够解锁 → 领奖，参数实时读当前玩法配置，并说明灰框状态就能试玩。③ 风格参考改成在对话流里直接给一张样张图（AssistantMessage 新增 image 插槽），右侧不再为它切视图、素材板也不再多一个「风格参考」批次（批次回到 6 批，26 项计数不变）；确认后才切到素材库视图一批批生成。④ 存为活动模板的 toast 收成一行。⑤ 存完模板后首页「H5活动页」这一栏最前面会出现这张模板卡（带「我的模板」角标，hover 是「用这个模板」），不再只藏在输入框的 @ 里。⑥ 引用模板不再先闪一下夯爆了成品再换视图 —— 去掉 template 阶段，右侧一进来就是「左边模板页面 + 右边替换清单」的对照视图，清单每项带勾选框，可以取消不想换的项。⑦ 替换改成分 3 批执行（背景与主视觉 / 卡池与卡面 / 档位·任务·副玩法），每批之间停下等确认，清单里按批标「已替换」并有进度条，不再一步到位。⑧ 复刻链路最后一步的工具条标题显示「活动预览」。夏日冲浪的 H5 与右侧 Inspector 沿用此前从 Marketing King Campaign Studio 迁移过来的那套，未再改动。全链路浏览器实测（未提交）

- 侧栏产品头按产品分档：创作者中心不要产品头也不挂底部操作（顶部直接是「发布作品」，flushHeader 关掉恢复 12px 上内边距，顺带删掉只有方案 4 能走到的 icon 头死分支）；百科头部文案定为「百科目录」+ 右侧收起，底部保持「我的词条」；随变维持「开启创作」+ 收起。另 /sidebar 规范画布的组件配置面板默认收起。浏览器实测（未提交）
- 打通「存为活动模板 → 引用模板生成新活动」链路，并把玩法配置讲清楚：① 预览工具条的「设为模板」改为「存为活动模板」，成品态和复刻态都能存；② 首页「营销活动」入口新增「模板」下拉槽位（没存过模板就不出现这个选项），选中即把 @token 带进 prompt，和输入框里 @ 引用同一条路；③ 复刻链路重做 —— 目标从虚构的「这春嗨翻了」改成真实的「夏日冲浪 · 顺风顺水」，该项目从侧栏预置列表里撤掉，只有引用模板生成后才作为新项目出现；流程变成 读模板（右侧摆出被引用的模板成品）→ 出「替换清单」（新增 XiahuaCloneDiff：素材 7 项 / 玩法 6 处，逐条 模板里是什么 → 新活动换成什么，并说明没列出来的结构原样继承）→ 卡点确认或调整 → 按清单替换 → 右侧直接变成真实的夏日冲浪 H5；BuildPhase 加 template / diff，回放归属改用 xiahuaBuildOwner 判断（复刻目标有自己的预览组件，不能再靠 isXiahuaFamily）。④ 活动玩法配置整页顶部加「这个活动怎么玩」一段话 + 关键数字标签（从 gameplay 实时算），卡池每行挂上这张卡的卡面缩略图，素材没出或缺图时是空槽。顺带修掉最终阶段工具条标题的判断（原来按 label 文案匹配，改名后失效）。全链路浏览器实测（未提交）
- 素材清单从一列干名字改成能对上界面：每项标出所在页面与区域（头图 / 主按钮区 / 集卡面板 / 卡槽 / 任务区…），面板够宽时并排一张页面框架缩略图，按 XIAHUA_EDIT_TARGETS 的真实槽位坐标画框打编号，划过清单某行对应的框高亮、反之亦然；编号按页面分段连续。另外按反馈去掉「第 N 批」的呈现（生成时整份或单项都行，不该强调批次），follow-up 文案改成标准操作建议（生成页面框架 / 接入核心玩法 / 生成素材清单 / 生成风格参考 / 按此风格生成全部素材 / 生成 UI 界面），去掉「可以」「没问题」这类语气词；点 follow-up 直接发送，不再逐字打进输入框。进编辑画布默认对准刚才预览的那一屏、保持与预览相同的缩放，不再缩成 5 个画板的全景（未提交）

- SideNav 收起入口统一挪到顶部产品头右侧：规范画布（/sidebar）去掉底部「收起导航」行，footer 改为业务自定义示例「偏好设置」，各列（主标注 / 创作者中心 / AI 工坊 / AI 分身 / 收起态 rail / 布局蓝图）补上 40px 产品头（文案 + 右侧收起，标注几何跟着加一段）；百科方案 4 头部由「对象切换 + 文件 + 收起」改为「管理百科 + 收起」一行，对象切换与文件入口下沉到第二行（设计稿 统一导航 583-5262）。浏览器实测画布与百科收起均正常（未提交）
- 默认导航方案改为方案 4「增加 Header」（DEFAULT_NAV_VERSION），nav-version persist 版本提到 2，旧存档一次性迁到方案 4；浏览器实测新访客与旧存档均落在方案 4（未提交）
- 0→1 回放按真实搭建顺序重排成 9 段，另修四处对话流呈现：① 阶段改为 方案 → **页面框架**（只有版式热区）→ **核心玩法**（框架占位上跑通抽卡/集卡/兑奖，手感在没出素材时就调掉）→ **素材清单**（按框架槽位倒推 26 项，可增删）→ **风格参考**（先出一张样张定调，认可后才批量）→ **分批出素材**（可换版、可本地上传替换）→ **生成 UI 界面** → **调 UI**；BuildPhase 加 gameplay / assetList，线框态覆盖 wireframe+gameplay，素材板首批是不计数的风格样张，新增 XiahuaAssetManifest 渲染清单，素材卡加「本地上传」（FileReader → dataURL 作为新一版排在生成版本后并自动选中）；静态生成记录同步重写。② 卡点按钮不再是黑色主按钮 —— 一律描边弱色，并挂到提出它的那条 AI 回复下面（AssistantMessage 加 footer 插槽），不再单独占一块。③ 产物卡片给图标：DOC/框架/玩法/清单/素材/H5 各配 icon + 弱色类型 chip，原来纯字母角标看不出是什么。④ 右侧「活动方案」去掉「解析结果 · 可编辑」「待确认 · 可直接改」这类编辑提示，标题收成「活动方案」。另外首页传策划文档现在新建一个活动项目从零搭（不再顶掉已有项目），原来那版改名为「这夏夯爆了 · 已上线」保留为成品样板与模板来源；顺带修掉侧栏用 PROJECT_KINDS 判 kind 导致会话内新建项目被当成小程序、长出「兴趣卡配置」的问题，以及回放收目录会把旁边项目一起收掉的问题。全流程浏览器实测（未提交）

- 按 Marketing King Campaign Studio 原版右侧 Inspector 完整迁移夏日冲浪面板：补齐 P1 跳转事件、P2 主题草稿复制/导入/删除、G2 7+14 色彩 token、M1 首尾帧与高级素材、M2 Hero 图层组合画布/解锁方式/坐标缩放/视频过场/批量上传/奖励高级样式，以及 M3–M7 全字段；Hero 组合图层同步渲染到活动预览，工具栏仍只保留单一「编辑」入口（未提交）
- 按 Marketing King Campaign Studio 原有右侧 Inspector 结构接入夏日冲浪编辑面板（Project P1/P2、Global G1/G2、Modules M1–M7），补齐副玩法与活动 Banner 的可编辑数据联动；夏日冲浪编辑入口改为与「这夏夯爆了」一致的单一「编辑」，移除画布编辑器（未提交）
- 夏日冲浪活动接入专属编辑链路：预览态新增「快速编辑」与「画布编辑」入口，右侧属性面板支持主视觉主题、抽装备按钮、装备册、金豆副玩法、任务、话题和热门投稿区块的点选与实时文案配置，画布态与快速编辑共用配置（未提交）
- 头图 KV 改成多素材图层合成（对齐 Figma「活动收集/head」的真实结构）：从 Figma 分层导出 空场景底景 + 小马角色 + 8 个食物/心仔贴片（raw 透明底图，压缩后 ~740KB 落 public/assets/xiahua/kv/），ActivityPreset 新增 kvLayers（id/file/label/坐标/z 序照抄设计稿），预览里 v1 默认态渲染 KvComposite 逐层叠放（贴片 404 静默隐藏、底景缺失才占位），挑整图候选（v2+）或线框态退回单图（未提交）
- 新增模板复刻全流程（活动做完 → 设为模板 → 首页 @ 引用 → 换皮生成新活动）：① 预览工具条新增「设为模板」（toast + localStorage 注册）；② 首页输入 @ 弹出模板引用层（未注册给指引文案），选中插入 token；③ 提交识别 token 后创建「这春嗨翻了」项目并播 TEMPLATE_CLONE_SCRIPT（读模板 → 换背景 → 换形象 → 换素材 → 完成，每步 preset mutation 真改：底景换 ACG 春日 hero、主角换新春小马、主题色切青蓝、标题字占位、阶段文案换春日版，玩法/页面/贴片全继承）；④ BuildMutation 加 preset 类型、XiahuaBuildFlow 支持自定义脚本与用户原话覆盖、@模板 的原话作为首条消息；⑤ 模板与复刻品共用活动机制（isXiahuaFamily 路由），切回模板项目自动归位状态。全流程浏览器实测（未提交）
- 去掉进入编辑态右上角的默认操作提示（XiahuaEditPanel 顶部那块蓝色引导：点选元素/拖动/缩放说明），面板直接从 3D 工作台入口开始（未提交）
- 修抽卡链路缺图：FoodCardFace 原来按 img/imgGrey 是否为空走「另一态 + 滤镜近似」的兜底，但两个字段都是按约定无条件拼出来的路径，永远非空，文件缺失（6 种卡无石膏版、卤味/螺蛳粉无彩色版）时 404 直接落到虚线占位框。改成按加载结果回退：主选态 onError → 换另一态并加滤镜（缺石膏 → 彩卡灰化，缺彩卡 → 石膏暖化），两态都失败才显示占位。主会场九宫格 / 卡册 / 抽卡动画 / 开卡结算共用此组件，一处全修。实测卡册 9 张全部有卡面（未提交）
- 交互框架阶段改成真正的黑白线框图：线框态素材位本就全部置空，在预览根容器加一层 grayscale(1) invert(0.92) 滤镜，深棕活动皮肤翻成浅灰底、白字翻深字、红按钮和彩色角标全部变中灰 —— 只剩版式与热区，不再像上了色的低保真稿；编辑画板 5 个屏同组件同生效，彩色阶段不受影响（未提交）
- 首页上传再改三处，把 0→1 的时间线守住：① 上传的文档回显到输入区顶部的附件卡（ChatComposer 新增 attachments 插槽，豆包式卡片：图标 + 文件名 + 类型/大小 + 移除，输入框随之加高），不再挤在底部工具行；② 进项目默认全屏聊天 —— phase 兜底从 doc 改为 none，右侧要等「读完出方案」那步才展开，首页跳过前两步进来也一样；③ 修掉发送瞬间闪出成品 UI：打字动画期间就置入搭建态（step 就位、路径为空 = 空对话 + 右侧收起），不再短暂渲染静态历史和默认成品预览，脚本回放入口同步处理。实测：发送后全屏读文档，方案出来右侧才开（未提交）
- 首页上传链路对齐真实上传五处：① 用户敲的原话不再被脚本吞掉 —— 命中活动流程后从脚本 doc 步直接开场，原话 + 附件是同一条消息（startXiahuaBuild 加 userText，XiahuaBuildFlow 加 docText 覆盖）；② 右侧《活动方案》头部「来自 xxx.docx」改用真实上传文件名（XiahuaPlanDoc 新增 docName，解析骨架同步）；③ 没命中活动关键词的附件不再静默消失，至少以「【已上传文档：名字】」前缀带进对话；④ 附件 chip 由 shrink-0 改可收缩截断，不再把工具行挤到「游戏素材」和 Auto 重叠；⑤ 只有一版候选的素材禁用按钮 title 从「已到版本上限」改为「这一项本轮只有一版候选」。顺带修掉「再放一次/▷ 回放」把 MouseEvent 当 docName 传入的隐患，回放会带着上传的文件名与原话重放。全流程浏览器实测（未提交）
- 新增 docs/这夏夯爆了·夏日集卡活动策划.docx：模拟用户原始上传的策划文档（内部沟通版形态，与 XIAHUA_PLAN_MD 解析结果逐段对齐）。配图不用任何成品 UI —— 原始需求里不该出现 AI 产出的最终视觉，改为 3 张铅笔手绘草图（卡面彩色/石膏双态示意、主会场结构线框、小马 IP 标注涂鸦），演示时可直接从首页上传（未提交）
- 新增「这夏夯爆了」完整活动需求 DOCX：覆盖 3 个候选玩法、9 张卡、4 档奖励、5 个页面、26 项素材、多版本选择与 AI 工坊 0→1 模拟要求（未提交）
- 首页支持真实文档上传并接入「这夏夯爆了」0→1 搭建回放：上传活动策划文档后从首页进入方案解析、框架、素材与成品流程，附件显示真实文件名（未提交）
- 素材详情内增加右侧版本缩略图列：支持直接点击选中版本，详情主图同步切换；素材库外层不展示版本列（未提交）
- 收窄右侧预览顶部的活动页面组件下拉：选择按钮按页面名称自适应，菜单宽度调整为 124px（未提交）
- 按参考图重做「这夏夯爆了」小马 IP：替换为红色圆润玩具体、深棕色鬃毛/尾巴、米色口鼻/手脚与半睁眼表情，并接入 3D 工作台、活动预设和素材目录（image2 + 抠图，未提交）
- 「这夏夯爆了」页面选择改为组件内自定义菜单，去掉浏览器原生下拉；工具条按钮收窄为 112px，只显示当前页面名，菜单内保留 5 个页面与当前选中态（未提交）
- 右侧预览工具条为「这夏夯爆了」增加页面下拉选择：主会场 / 开卡结算 / 我的夜食 / 兑奖弹窗 / 活动规则，切换同步非编辑预览和编辑画板（未提交）
- 用 image2 生成独立小马 IP 3D 渲染并新增「小马 IP · 3D 对象工作台」：支持拖拽旋转、俯仰/大小/灯光/背景调整与 PNG 截图输出；活动奖品 IP 同步替换为小马渲染（当前为可运营截图的 2.5D 对象，未提交）
- 用 image2 按「这夏夯爆了」风格生成真实素材候选：主视觉 4 版、标题/集卡面板/主按钮/开卡大卡/火锅卡/43 元券包各 2 版；素材板与预览改为按真实文件切换，替换掉滤镜伪版本（未提交）
- 三处跟回放过程对齐：① 左侧目录跟着过程长 —— 聊需求和出方案阶段只有「项目文档」（就是上传的那份策划），界面搭出来才出现「项目文件」，素材开始生成才出现「素材库」，玩法接上才出现「活动玩法配置 + 数据库」；不在回放中仍然全给（侧栏新增 hiddenCategories 入参，按 build phase 过滤产物目录）。② 素材版本改成攒出来的 —— 原来默认每样就有 v1/v2/v3，现在每样先出 1 版，点「再出一版」才涨（上限 3），脚本的换版轮也从「换成第三版」改成「各再出一版」并同时涨 versions；预览侧的调色表跟着对齐（第 1 版 = 原图）；相关文案全部改成「每样先出一版，不满意再出」。③ 版本序号重排 —— 原来 v1/v2/v3 三颗挤在名称下面一行，加上素材宽高比差异大导致每张卡的那一行高度都不一样，一排看过去很乱；现在缩略图定高 96px（object-contain 居中），版本号做成贴在图右上角的 v2/2 角标，下面一行是版本切换 + 「再出一版」，只有一版时显示「只有这一版」。实测：目录三个阶段依次长出、换版后头图与标题显示 v2/2、成品后目录补齐 5 项（未提交）
- 按真实策划文档（飞书 wiki「抖音ACG新春会项目策划」）的形态重做解析结果 —— 真实文档不是规整的参数表：前三分之一是项目定位、数据指标（一堆 xx 占位）和竞品参考，真正能落地的玩法散在「活动形式」下面的「内容呈现 / 用户参与 / 激励形式 / 页面展示」几个小标题里，时间是几个阶段节点而不是一个档期。相应改动：① PlanDoc 结构从「档期/主题/目标」扩成三层——背景（项目定位、核心目标、人群、数据指标，标注不影响页面搭建，xx 占位标黄）、能落地的（候选玩法带原文出处、卡池、档位、机会来源、要做的页面）、待确认 + 新增「仅作背景」区（读到了但这次不做进页面）；活动节奏改成预热/主推/收官三阶段表。② 解析的思考改成读真实文档的动作：先说明前三分之一跟页面无关但要记、xx 占位先标出来不卡搭建、玩法要从几个小标题里一条条挑、时间是三个节点会影响要不要做倒计时。③ 项目里的《这夏夯爆了项目策划》整篇按同一形态重写（项目定位及目标 / 数据指标 / 活动形式分模块 / 活动时间表 / 待确认），附件卡也从「28.6 KB」改成「12 个小节 · 4 张图」。顺带修掉回复里 markdown 加粗星号被原样渲染的问题（未提交）
- 对话流的消息配对修了四处：① 开头那条「想做个什么活动？」是没人说话时 AI 凭空发言 —— 删掉，对话从用户开口开始（回放起手同样先把字打进输入框）；② 附件和说明原本是两条连发的用户消息，合成一条（DocAttachment 支持 text，共用一个时间戳）；③ 点确认按钮后直接进下一段 AI，等于 AI 连说两条 —— 现在每个确认都会落成一条用户消息（ok-plan / ok-wf / ok-asset，放在两条支路的汇合点），按钮文案与气泡文案统一；④ 静态记录里三处 AI 连发，补了对应的用户消息。另外发现一个渲染 bug：对话按「0..当前下标」区间渲染，而卡点会跳步，导致点确认后没走的「我要改」支路也会显示出来 —— 改成记录实际走过的路径（xiahuaPath）按路径渲染，时间戳也按路径位次算，阶段派生量同样改成按路径取。两条路径（全确认 / 全走支路）脚本级校验：首条都是用户，无连续同角色。顺带把右侧「交互框架 · 版式与热区」改成「界面结构 · 位置已定 · 暂无视觉」（未提交）
- AI 工坊首页入口调序:兴趣卡提到最前,末尾加「更多」浮层(网页游戏/小程序/AI 分身/运营提案);游戏素材补精灵帧动画、序列帧特效等类型及帧数/交付槽位(未提交)
- AI 工坊首页下拉槽位移到工具条左侧、紧跟蓝色入口排列,中间不再留一大段空白;下拉改左对齐展开(未提交)
- AI 工坊首页快捷入口的子能力也收成下拉槽位,选中类型后统一是四个下拉(玩法/形态/参数…),不再有平铺 chip(未提交)
- AI 工坊首页快捷入口改成豆包式:选中入口变蓝并可 ✕ 退出,工具条换成该类型的子能力,右侧 Auto 换成该类型参数下拉,选择结果作为前缀带进 prompt(未提交)
- AI 工坊首页快捷入口去掉「Agent模式」,活动页面→营销活动、兴趣卡模板→兴趣卡;输入框光晕改用外阴影,不再从磨砂面板里透出来(未提交)

- 项目库整页替换为 AI 平台 (ai_design) 的版本：33 条项目数据从 AppListPage.tsx 原样搬到 projects/projects-data.ts，13 张封面 + 作者头像压成 webp 进 public（392KB）；页面按 AppListPage.module.scss 重建（左 216 三空间×五类型锚点导航、hover 换折叠箭头，右侧 info banner + sticky 筛选条 + 128 高卡片四列栅格，hover 彩色模糊描边）。原来挂在项目库菜单下的 AgentHubPreview 已替换（未提交）
- 资源库整页替换为 AI 平台 (ai_design) 的版本：resourceMockData.ts 全量（2486 行，含工具箱/知识库/模型库/发布器/触发器五套数据）搬到 resources/resources-data.ts，30 个 asset import 改为 public 下压缩后的 webp/svg（66 个文件 1.5MB）；页面合成单组件 + 五个 tab（原来是五条独立路由），工具箱保留左侧分组树与计数，各 tab 的筛选项与分组逻辑对齐源站。原 ResourceHub 已替换（未提交）
- 技能库整页替换为 AI 平台 (ai_design) 的版本：数据（类型 / 分类树 / 分组卡片，含原 CDN 封面）从 mocks/mockData.ts 原样搬到 skills/skills-data.ts；页面按 components/skills/SkillsPage.tsx + routes/index.css 的 .skills-* 规格在 Tailwind 上重建（左 216 分类栏含广场树/小花粘底/团队/个人，右侧筛选条 + auto-fill minmax(218px) 卡片网格，卡片 191.57 高、封面 93.43）。原来挂在技能库菜单下的 ResourceLibraryView 及其专用 state / useCapabilityInChat 一并移除；icons.tsx 补 PlusCircle、Puzzle（未提交）
- 今晚三件：① 属性面板逐个对象做差异化 —— 原来只有 7 个元素有专属面板，其余（KV/两个侧入口/金豆条/三个分区/页脚 + 新加的 17 个弹层元素）全落到同一个「素材」面板。现在 30 个对象各有各的：KV 给画面内容（主体/场景/色调/安全区）、侧入口给跳转与角标、金豆条给文案与金豆数、任务区给任务来源、话题区给内容来源与排序、banner 给投放位与时段、结算页给卡面来源与入场动效、卡册给页签与三态按钮文案、兑奖给文案模板与实物奖流程、规则给弹窗样式与正文（可编辑 textarea）。弹层元素不给「位置与缩放」和「删除」（位置由布局决定、删了也不生效），改成一句说明；图层清单也跟着当前画板换（点开卡结算就列它那 5 个）。② 对话流不再体现我们的实现逻辑 —— 静态记录里编辑器能力那几轮（拖拽/缩放/流式分区/绑定/活动模板）整段删掉，换成继续打磨活动本身（收档位、调抽卡手感、改进度文案、档位状态动态化、卡册赠送三态、加投稿入口、修规则弹窗按钮）；回放脚本的思考也从「槽位/绑定/按 key 引用」改成产品判断（为什么砍最后一档、为什么概率和次数要一起调、为什么标题得跟着头图换）。③ 开头与输入框结合：回放前几步只是聊需求，右侧不开（新增 phase none，派生 previewCollapsedEff），等解析出方案才展开；用户每句话先一个字一个字打进输入框再发出去，打字期间输入框锁定（未提交）
- 工坊首页输入框去掉预填片段，改成普通 placeholder（「说说你想做什么，例如：生成一套炉石风格的游戏卡牌」），空内容时发送键禁用、只发用户实际输入（原来空内容会发出预填的那句假 prompt）。实测输入并发送后确实新建了项目并进入对话（未提交）
- 这轮五处：① 对话流去掉「设计稿」概念 —— 项目记录重写成纯自然语言创建的过程（一句需求 + 一份策划文档 → 拆解 → 框架 → 素材 → 合成 → 迭代），删掉所有对图稿/切图/逐像素还原的引用，属性面板的「设计稿位置」也改成「位置」；对话样式抽成 XiahuaChatUI 由回放和静态记录共用。② 编辑画布撑满视窗：fit 去掉 ≤100% 的上限、内边距 48→20、画板间距 40→28（5 个画板整体铺满，1680 宽下 45%）。③ 去掉画布左下角的操作提示，缩放控件放右下角。④ 活动规则弹窗的「我知道了」不再被裁：弹窗改成 flex 列，正文滚动、按钮固定在底部。⑤ 另外 4 个画板的元素也能选中了 —— 新增 SCREEN_TARGETS（开卡结算/卡册/兑奖/规则 共 17 个热区），在弹层之上加一层视口坐标的热区，只可选中不可拖。另做了两处性能收敛：XiahuaH5Preview 与画板行 memo 化（平移缩放只改外层 transform，screens 补 useMemo），只读画板停掉常驻的无限动画 —— 结构性优化，未实测帧率（未提交）
- 产品名回退：只有独立版（方案 7）叫「抖音 AI 工作台」，其余方案一律「AI 工坊」—— 顶栏产品入口 / 首页产品卡 / 加载态 / 作品来源 / 助手跳转 / AgentHub 演示文案 / Kimi 与分身生成 system prompt / 规范画布全部改回；侧栏 aria-label 改为随方案切换（独立版 AI 工作台侧栏，其余 AI 工坊侧栏）（未提交）
- 工坊首页背景与顶图改用 AI 平台 (ai_design) 线上那套：ASCII 底纹从静态贴图换成 CreatorAsciiTexture 的 canvas 实现（1024² alpha 遮罩逐格采样 + 12fps 微流动，参数原样搬过来，新增 src/modules/vibecoding/components/AsciiTexture.tsx）；hero 顶图换成 ai-workshop-hero-ring-creator-center（磨砂玻璃质感，Figma 单节点导出会把左下那颗渲染成生硬深色照片），定位照搬 .heroRing 的 945 宽 / top -24 / 居中；根容器补 isolation:isolate —— z-[-2] 的 canvas 只有本层成为层叠上下文才不会被祖先背景盖掉；页面渐变底两边本来就一致。新资源压到 hero-ring.webp 31KB / ascii-mask.webp 55KB，删掉旧的 hero-cluster.webp 与 ascii-map.webp（未提交）
- 会话头精简：删掉分享和历史对话两颗按钮（连同 historyMenu 的 state/ref/关闭 effect 一起清掉），0→1 回放从对话流里的那张蓝卡改成会话头右上角的 ▷ 图标（只在「这夏夯爆了」出现，回放中高亮）；生成记录组件不再需要 onReplay。实测头部只剩「新建对话 + 回放」两颗，点 ▷ 能正常从零回放（未提交）
- 工坊首页顶部按设计稿逐项校准：纵向节奏改用内容面板绝对坐标（椭圆簇 top 90 / 标题组 top 274 / 输入框 top 368，原来是 42/136/320）；副标题 14px→16px 并补 tracking 0.32；输入框内 prefill 行与 textarea 文字对齐到同一个原点（面板 +25/+21，textarea 内距 px-1 pt-1→px-3 pt-2），敲字不再跳位；空态由 prefill 行承担，去掉多余 placeholder（未提交）
- 0→1 回放三处修：① 每条 AI 回复顶部都补了生成过程（新增 8 段 think：看项目现状 / 改方案 / 改框架 / 换素材版本 / 收档位 / 调抽卡概率 / 改文案模板 / 汇总本轮改动），之前只有解析、搭框架、生素材、合成四处有；② 删掉生成记录里所有绿色「已应用到预览 / 已应用到项目」徽章；③ 编辑态画布不再自绘底色和网格 —— 原来它在预览画布的点阵上又盖了一层 surface-1 底 + 随缩放变密的网格，所以进编辑「背景变了」，现在直接沿用外层画布，进出是同一个；④ 5 个画板全部可编辑：点哪个画板（或它的标题）它就成为当前画板，蓝框 + 编辑中标记跟着走，同一时刻只有一个在编辑态，非当前画板 hover 有蓝边提示。实测切到「我的夜食」「兑奖弹窗」都能接管编辑态（未提交）
- AI 工坊首页按 Figma 探索 151:12832「首页入口（方案2）」重做：hero 椭圆图片簇（节点整导出裁到可见弧段）+ 创所未见·AI工坊 标题；输入框改为预填 prompt 片段（图片 chip / 虚线槽位），工具行换成 Agent模式 + 活动页面/设计素材/游戏素材/兴趣卡模板；新增「没有灵感？」6 个需求 chip；底部新增 10 个分类 tab + 5 列灵感作品网格（179×322 卡面 + 作者/收藏行，hover 出「做同款」）。原兴趣卡 banner / 兴趣卡 / 精选项目三段按设计稿移除。卡面与头像用 Figma 原图压成 WebP（共 ~380KB）落在 public/assets/workshop/inspire（未提交）
- 0→1 回放的对话流样式对齐 ~/Downloads/AI 平台/ai_design 的 ProjectDetailPage：用户消息右侧气泡（fill / r12 / 14-22 / max 520 / 10-14 内距）下挂时间戳；助手消息无气泡，结构是「✓ 已完成xx ›」状态行 → 13-22 正文段落 → 产物卡片（38×38 彩色 badge + 标题 + 说明 + › ，DOC/WIRE/IMG/H5 四种色调）→ copy/引用 操作行 → 时间戳。状态行就是原来的思考块：跑时是转圈 spinner + 「读策划文档…」，跑完变绿勾 + 「已读策划文档」，点开看推理明细。四张产物卡分别挂在方案 / 框架 / 素材 / 首版四条回复上。时间戳按步序派生，不用真实时钟（未提交）
- 0→1 回放的对话改成正常聊天：删掉编号阶段横幅、清单卡片、进度条这些「步骤提示」，只剩用户气泡 / AI 回复两种；过程收进灰色可折叠的思考块（连续同 group 的 think 步骤自动合并成一块，跑的时候标题呼吸「读策划文档…」并逐行长出，跑完自动折叠成「已读策划文档 ›」，点还能展开回看），结论用正常正文；卡点选项变成跟在回复后面的两颗按钮，不再包卡片。脚本内容同步重写成第一人称的推理与结论，思考里写为什么这么判断（比如为什么先定框架再出素材、为什么插入走自动排布），结论只说做了什么。全流程实测（未提交）
- 工作台首页输入框背后的深色光晕收小：inset-x 52→110、top 26→42、bottom -6→8，实测由 696×147 缩到 580×117，且不再从输入框底边溢出，只在边缘渗一点（未提交）
- 0→1 回放：① 对话流里的选项全部去蓝改墨色（卡点确认/次要按钮、起点选项、阶段序号、生成中指示、素材版本 chip、方案文档里的勾选与聚焦态、开始回放入口）；② 右侧按阶段给对应视图而不是统统套画布——方案/素材走整页平铺（无点阵底、无缩放控件、无编辑按钮），框架/成品才走画布（点阵底+手机/画板+缩放+编辑）；工具条标题也随阶段变成「活动方案·解析结果可编辑 / 交互框架·版式与热区暂无素材 / 素材·按框架槽位产出多版本 / 活动预览·素材已合成」。顺带把素材缩略图底色从棋盘格换成活动主色，透明底的白字标题不再看不见。四个阶段逐一实测（未提交）
- 主按钮高度统一走组件配置的 36：删掉方案 7 两处挂载点上写死的 height:32（圆角 10 / 字重 500 保留），侧栏主按钮不再在挂载处覆写尺寸；side-nav-config 补一条迁移，把浏览器里存过的旧默认 buttonHeight:40 归到 36（与 bg / collapsedWidth:61 / rowSpacing:4 同一套迁移机制）（未提交）
- 重排「这夏夯爆了」的 0→1 流程，改成：说需求/传文档 → 解析成结构化《活动方案》（右侧新增可编辑文档，字段直接改、勾选决定本期玩法范围）→ 确认 → 先出交互框架（新增 wireframe 态：素材位全变带名字的占位框，只有版式/热区/玩法绑定）→ 确认 → 才生成素材（新增素材板，按框架槽位分 6 批产出，每个素材 3 个候选版本可切换）→ 确认 → 按 key 落位合成最终活动 → 迭代。三处确认是真卡点（脚本加 gate，播放停下等人点，confirm/alt 两条路径都真改状态）；回放时对话自动滚到底。实测六个卡点依次触发：改方案（投稿任务移出本期、三档改 43元券包）→ 改框架（删 banner、标题上移）→ 换素材版本（KV/标题切 v3）→ 合成 19 张素材 → 迭代到 v1.1（3 档、还差 N 种文案、任务区后插引导入口）（未提交）
- 方案 7 更名「独立工作台」→「内部抖音 AI 工作台」（导航方案菜单 + nav-version 注释里残留的「工坊」一并改掉）（未提交）
- 「这夏夯爆了」抽出活动模板层 ActivityPreset：素材目录/主题色/阶段/文案/玩法全部从 preset 读，预览不再写死任何路径；素材按约定命名（head-kv/title/btn-draw/panel-bg/tier-1..4/card-<id>[-grey]/sec-*/footer-logo）放进 public/assets/<id>/ 即可，命名不符的用 assetOverrides 映射（这夏夯爆了即复用现有文件名）；所有 img 换成 ImgOrPlaceholder，缺图显示带名字的虚线占位而不是破图；「活动玩法配置」加模板切换，内置 这夏夯爆了 / 空白模板。实测切到空白模板整页变成带占位的新活动骨架、玩法与角标照常，切回来 20 张 xiahua 素材全部复位（未提交）
- 方案 2（内容区收起）入口位置定死在各产品 Header 左侧：按钮 size-7→size-6，配 top-2 在 40 高的 Header 里正好垂直居中；三处 Header 左内距 pl-[52px]→pl-12（12 入口左距 + 24 按钮 + 12 间距）；随变 Header 原本 h-14（56），该方案下改成 h-10 与其他产品对齐。四个产品实测 Header 均为 top 48 / 高 40 / 左内距 48，入口均为 [内容左+12, 56, 24, 24]，上下各留 8（未提交）
- 顶部工具条方案的收起/展开统一：收起态不再换成产品 logo 头，而是沿用同一条 UnifiedToolbar 只留「收起导航」一颗（新增 collapsed 入参过滤 actions），工坊/百科/随变三处一致；工具条里 layout 那颗也换成 SideNavPanelStateIcon（原来是 assets/unified-toolbar/layout.svg 实心面板图、写死 #1C1F23 无法继承色）。实测收展两态该按钮都在 [12,68,24,24]、同一枚图标，下面 AI 创作与菜单行坐标也完全一致；顺带 aria-label 随状态切换，并移除已无用的 SideNavProductHeader.collapsedRowHeight（未提交）
- 导航方案 2 与 8 序号互换：2 = 内容区收起（原 8），8 = 顶部工具栏（原 2）。散落的 navVersion === 2 判断改走具名 helper —— 新增 usesToolbarHeaderLayout（=8），usesContentToggleLayout 改为 =2，百科/随变的收起轨条件与内联展开按钮排除项一并补上两个方案（未提交）
- 方案 8 侧栏顶部不再放产品 logo：把方案 8 移出 usesProductHeaderLayout，各产品侧栏顶部直接就是自己的第一件东西（工坊/分身=AI 创作按钮、百科=对象切换器、首页=发布作品），收起入口本来就只在内容区左上角（未提交）
- 新增导航方案 8「内容区收起」：收起/展开唯一入口由外壳统一渲染在内容区左上角（跟随侧栏宽度动画），各产品侧栏里不再放收起按钮，位置对所有产品完全一致；首页不提供收起（强制展开且不渲染入口）；侧栏顶部保留产品 logo；分身/工坊对话 Header、百科标签栏、随变标题栏在该方案下补 pl-[52px] 给入口让位（未提交）
- 导航收起/展开图标统一换成 master-icon 的 sidebar-hidden-left-wide 线性图标，收展两态同一枚（原来是 public/icons/layout 那对实心面板图，24 视口里只占 12.5px 又是实心，混在一排线性菜单图标里显小又显重）；图标配色一律实色 #565A60 / hover #161823，不再用 text-[#252632]/45 这类透明度填充；方案 5 底部收起按钮展开态原本用的是 « 双箭头，一并统一（未提交）
- 方案 2 顶部工具条到下面第一项的距离各产品不一致（工坊 12 / 随变 12 / 分身 0 / 百科 0）—— 分身的 mb-3 只给了工坊，百科压根没给；统一成 12（未提交）
- 方案 6 搜索框改成与相邻的对象/项目切换器同款（h-8 / rounded-lg / #e9e9eb 描边），原来是 rounded-full + 蓝调 ring，在百科里和下面的「灵笼」切换器上下贴着，一个胶囊一个圆角卡不像一套；同时补上 12px 间距（未提交）
- 方案 4 首页不再提供收起：SideNavProductHeader 的 onToggle 改为可选，不传就不渲染收起按钮（收起态也只渲染 logo 不再挂「展开导航」语义），首页在方案 4 / 8 下强制展开（未提交）
- 全导航方案收展对齐排查并修复：方案 1 品牌区（收起层原本在展开宽度里重新居中，展开时 logo 先右窜 80px 再淡出、两层交叉淡化还发灰）改为「纯 logo 常驻左对齐不淡化 + 带文字字标单向淡入」，两个 SVG 的音符路径同坐标同尺寸所以叠加无缝；方案 2 收起态顶部头比展开态 UnifiedToolbar 矮 8px（SideNavProductHeader 新增 collapsedRowHeight）；方案 6 搜索工具条的 mb-3 只在展开态给，收起时下方菜单上跳 12px。方案 3 / 7 实测两态完全一致；方案 4 / 5 收起是整条隐藏（12px 余量、无图标轨无 logo），待定（未提交）
- 产品名「AI 工坊」→「AI 工作台」：侧栏品牌、首页 Hero、顶部产品入口、作品来源、助手跳转文案、加载态、各处 aria-label、Kimi system prompt 全部改名；240 宽下品牌行放不下新名字，gap 8→6、个人空间按钮 px-1.5→1、行尾 pr-1→0（未提交）
- 方案 7 收展去闪烁：两层交叉淡化改为「图标轨常驻不透明 + 展开层单向淡化（不透明底 #F2F2F7、z-10 盖在上面）」—— 原先两层各 50% 透明度叠加，logo/图标/底部头像这些同位同图元素合成后不到 100%，看着就是闪一下；同时把收起态菜单行的 justify-center 换成与展开态同一个 --sn-rip 左内距（居中会被滚动条占位挤偏 3px），收起态新建项目按钮同理改左对齐（居中差 0.5px）。现在 logo / + / 五个菜单图标 / 头像在两态坐标完全一致，零错位零闪烁（未提交）
- 侧栏「点名称不再收起」：项目行点名称只展开 + 打开预览，收起统一交给左侧披露箭头（无目录树的项目没有箭头，仍由名称切换）；FileTreeView 里的文件夹行同理，点标题只展开不收起（未提交）
- 方案 7 收起态与展开态逐像素对齐：收起轨的 logo 按钮 size-8→size-6（与展开态品牌行等高），底部头像补 h-8 行盒；logo / 新建项目 / 各级菜单 / 头像在两态的 x、y、行高全部相同，收展不再有位移（未提交）
- 方案 7 侧栏收展动效重做：外层容器自己承载面板底色并做 240↔60 宽度过渡 + overflow-hidden 裁剪展开层，删掉原先的 scaleX 底板；宽度、正文 margin-left / 会话列 left 统一走同一条 CSS 过渡（240ms cubic-bezier(.32,.72,0,1)），修掉正文瞬移、侧栏慢收的位移；两层改为非对称淡入淡出（离场 100ms、入场 180ms 延迟 60ms），不再糊在一起（未提交）
- 方案 7 独立工坊侧栏圆角 6→10（新建项目按钮 + 一级菜单行同档；项目树子行 8px、搜索小图标 4px 未动）（未提交）
- 方案 7 独立工坊侧栏宽度 280→240；SideNavItem 新增 dividerBefore，「项目库」上方补一条分组分隔线，与「项目列表」上方那条统一改为 0.5px（未提交）
- 这夏夯爆了画布补齐标准操作 + 新增可运行的 0→1 生成回放：XiahuaEditCanvas 改为真画布（滚轮平移、⌘/Ctrl+滚轮光标锚点缩放 15%–300%、空格/中键拖拽、适应屏幕、双击复位、网格随缩放，编辑态屏蔽外层 previewZoom 避免双重缩放）；新增 XiahuaBuildScript（22 步脚本：上传策划→拆解确认→素材分 6 批→组装→接玩法→6 轮调整，每步可带 stage 推进与真实 mutation）、XiahuaBuildFlow（对话侧逐步渲染、当前步显示生成中）；预览加 build prop 按 BuildStage 逐层显示（空白→KV→标题→按钮→集卡面板→下半屏）；6 轮调整真实改到 gameplay/overrides（3 档位、权重 88%、文案模板、标题上移、下线 banner、流式插入引导入口），静态记录页加「开始回放」、回放结束加「再放一次 / 查看完整改动记录」（未提交）
- 这夏夯爆了编辑态改多画板 + 玩法可见 + 可取消选择：新增 XiahuaEditCanvas（编辑时横向平铺主会场/开卡结算/我的夜食/兑奖弹窗/活动规则 5 个画板，主会场可编辑其余只读），预览加 screen/readOnly props 定格关键页面；点画板外空白、Esc、面板「取消选择」三种方式清除选中；带玩法绑定的热区加绿色 ⚙ 角标与标签页提示、图层行加「玩法」chip、面板空态加「玩法 · 6 条规则」总览（点击直达对应规则的玩法页签，经 XiahuaSel.openGameplay 传递）（未提交）
- 这夏夯爆了把玩法层从预览里抽出来并与 UI 编辑打通：新增 XiahuaGameplay（卡池/档位/抽卡规则/任务/赠送/文案模板 + GAMEPLAY_BINDING 元素↔规则映射）与 XiahuaGameplayEditor（面板按 section 显示、项目配置页显示全量）；预览改为按配置派生 FOODS/TIERS 与抽卡权重、赠送门槛、进度文案，卡面素材按 id 映射保持 UI/玩法分层；编辑面板选中带绑定的元素出「外观 / 玩法」双页签，改档位或卡池即时重算 UI；项目目录「活动玩法配置」与面板共用同一份 state（未提交）
- 这夏夯爆了画布编辑加删除与智能插入：新增 buildFlow 流式布局引擎（头部 668 + 分区/插入槽位依次排布，删除或插入后 y 与页面总高自动重算），buildTargets 汇总静态元素+实时分区+插入元素供热区与图层清单使用；XiahuaOverrides 增加 hidden/inserted；面板选中态加「删除」、空态加「插入元素」（图片/文本/按钮）与「已删除」恢复区；decidePlacement 按锚点自动判定 flow（流式分区→插到其后，后续顺延）或 overlay（头部合成区→叠加在锚点下方）并在面板标注依据（未提交）
- 这夏夯爆了画布编辑扩展为全元素可移动 + 可缩放：15 个热区全部支持拖动（绝对定位走 left/top+scale，满宽分区走 transform 平移不塌占位；进度文案/奖励档位收进编组整体变换），选中出四角手柄等比缩放 30%–300% 且对角锚点固定；XiahuaOverrides.offsets 增加 s 字段，面板「位置与缩放」实时显示 X/Y/百分比并支持重置（未提交）
- 这夏夯爆了编辑模式加画布拖动：8 个悬浮元素（标题/Tab/侧栏/三个按钮/卡槽/金豆条）编辑态按住即拖（pointer capture + 缩放比例换算，偏移以设计稿 px 记），元素与热区实时跟随；满宽切图标记不可拖动；松手经 onOffsetsCommit 提交 XiahuaOverrides.offsets，面板「位置」区显示偏移 X/Y 并支持重置位置（未提交）
- 这夏夯爆了编辑模式二次打磨：编辑图标改回原 quick-select（用 CSS mask 上色以跟随状态），激活态按设计稿 25-39783 用 #D4EBFF 底 + #357EF8 蓝字蓝图标、文案保持「编辑」；工坊侧栏默认展开项改为这夏夯爆了（ACG 收起）；编辑面板空态加选择引导卡 + 按页面位置分组的 15 项图层清单（带尺寸/类型，悬停联动高亮预览热区）；预览热区改为常显白色虚线框，悬停/选中转 #357EF8 实线+浅蓝填充并显示标签；选中态头部加「返回图层」（未提交）
- 这夏夯爆了编辑模式打磨：属性面板 header 改 40px 与预览工具栏同高、底线对齐；类型徽标与头图标按对象真实资产类型（图片/矢量对象/视频/文本/组件）对齐选中元素；「编辑」按钮激活态改蓝字蓝图标（Pencil + sky-600/sky-100）并显示「编辑中」（未提交）
- 这夏夯爆了新增预览编辑模式：工具栏「编辑」开关（开启后左侧项目导航自动收起、右侧停靠 XiahuaEditPanel）；预览按设计稿矩形给 15 个元素打点点选（悬停名称/选中框），面板按元素类型出针对性属性（图片=缩略图+Prompt+重新生成，按钮/Tab/档位/卡槽各有专属字段，统一 AI 修改入口）；剩余次数与当前阶段经 XiahuaOverrides 真实回写预览（未提交）
- 这夏夯爆了对照设计稿 1-4796 校正顶部：发现 Figma 导出把父画板深棕填充 #2F1912 烘焙进了 10 个切图（标题/抽卡按钮/两侧入口/金豆条/四档券/页脚字标），统一抠底为透明，标题改用内层纯字形组；阶段 Tab 与分享·规则栏改为真实可点元素（原整图切图删除）；PhoneMockup 传 381×818 抵消 3px 内衬，内容区回到足 375×812，标题/Tab/侧栏/抽卡按钮/集卡面板坐标与设计稿逐像素对齐（未提交）
- 这夏夯爆了项目构成真实化：新增 XIAHUA_ASSET_GROUPS（30 项实际素材分主视觉/卡面/奖励/交互/分区 5 组，每项带生成 prompt 与模型）接入素材库；项目文件树改为真实结构（docs / src / assets 四类子目录 / config）；ProjectObjectViews 补 基础信息·活动玩法配置·数据库（food_cards / reward_tiers / user_cards / draw_chances）；清理 2 个废弃切图并把火锅卡重命名对齐 food-* 规范（未提交）
- 这夏夯爆了细节调整：主页面顶部撑满（去掉地图条/状态栏与 KV 圆角底色）；「我的夜食」按设计稿 23-3784 重做（深棕底 + 装备卡/夜食卡/交换记录页签 + 绿色 xN 角标 + 赠送三态按钮），9 种夜食专属卡面全部替换进抽卡/卡槽/开卡/卡册（卤味、螺蛳粉暂只有石膏版，已获得态用暖色滤镜近似）；开卡标题抠掉烘焙黑底；左侧对话接入 XiahuaGenerationLog 预置「生成过程」记录，模拟平台从策划文档到可玩活动的对话链路（未提交）
- AI 工坊新增「这夏夯爆了」项目（marketing-h5）：按 Figma 设计稿（主页面/卡片浏览/开卡页）高保真还原夏日集卡 H5，XiahuaH5Preview 内闭环实现抽卡动效→开卡结算→卡槽点亮→集齐 2/4/7/9 种兑券/红包→我的夜食卡册/卡片浏览器/赠送/我的奖品/规则弹窗；素材从 Figma 导出（压缩后入 public/assets/xiahua，高清原件与参考视频归档 design-refs/xiahua 并 gitignore），设计稿仅有沸腾火锅卡面，其余 8 种卡用同底板+贴纸标签替换待后续素材生成（未提交）
- 全方案导航收起排查：百科收起态补回「灵笼」对象切换（封面图标 + 弹层，方案 1/2/3/6 图标轨），AI 分身收起态补回当前分身圆形头像入口；其余方案与页面收展回路验证通过（未提交）
- ChatComposer 支持挂载处覆写高度，工坊首页输入框加高到 166px（其余面板保持 114px）（未提交）
- 为工坊首页输入框补上设计稿的深色模糊光晕底衬，并将侧栏「AI 创作 / 发布作品」主操作按钮默认高度从 40 调整为 36（未提交）
- AI 工坊默认落地改回工坊首页（?project=/?page= 深链仍直达），并把首页输入框默认占位文案压缩到一行内避免被工具条截断（未提交）
- 去掉全部面板拖拽分隔的 hover/active 高亮线（仅保留光标与键盘聚焦指示），并将侧栏收起状态改为全局共享：任一页面收起后所有产品页默认收起（未提交）
- 将首页智能创作与作品发布 8 张入口卡全部改为前后层同步向左右展开（未提交）
- 加速首页智能创作与作品发布 8 张入口卡的悬停/按压反馈，并让首张 AI 分身卡双向同步展开（未提交）
- 全量校准 7 套导航收展锚点：移除所有横向补偿，方案 1/7 Logo 原位交叉淡化，统一首页、工坊、百科、随变图标至 30px 中心线，并让分隔线不再占用布局宽度（未提交）
- 重做方案 7 导航收展结构：展开层与 60px 图标轨常驻交叉过渡，背景独立收展并保留项目树状态（未提交）
- 完善方案 7 独立工坊侧栏：支持 60px 单列收起态、用户名改为 kingjaylee、移除“我的收藏”标题，并统一全部入口图标体量（未提交）
- 将头像下拉里的导航方案选择改为单列整行布局，完整展示每个方案名称（未提交）
- 调整方案 1 展开态收起按钮位置：左移并与下方菜单披露箭头共用垂直基线（未提交）
- 优化左侧导航展开/收起动效：统一 160ms 轻位移淡入，品牌 Logo 交叉过渡，百科/随变补齐分支切换动效，并同步 L 型圆角宽度（未提交）
- 按新「带文字 / 纯 logo」SVG 校正 L 型导航：收起为居中图标列，纯 Logo hover / focus 显示展开符号并需点击展开（未提交）
- 强化导航方案切换：方案 7 在账号下拉中独占一行；L 型侧栏收起后保留抖音图形，hover 临时展开完整左栏、离开后自动收回（未提交）
- 新增导航方案 7「独立工坊」：隐藏全局顶栏并直达 AI 工坊首页，按 Figma 重构 280px 工坊侧栏的品牌、新建项目、资源入口、收藏、项目树与账号区（未提交）
- 将 H5 快速编辑中的游戏与会场入口卡字段统一命名为「入口名称」，作品卡仍保留「作品标题」（未提交）
- H5 快速编辑选中组件或卡片时在右侧顶部展示对应真实预览，覆盖页面组件、三款游戏、主会场入口与五张作品卡（未提交）
- 移除 H5 快速编辑面板顶部的对象信息卡片，并让属性编辑内容直接从面板顶部开始（未提交）
- 按兴趣卡可见内容抽取星座、关键词、牌面、解读与按钮文案，并替换 Figma「探索」8:4806 属性面板的通用排版占位字段（未提交）
- 将方案 6 搜索框占位文案统一为「搜索」，并按要求将输入框高度调整为 32px（未提交）
- 新增方案 6「搜索工具栏」：沿用方案 2 框架并按 Figma 统一搜索框覆盖百科、随变、AI 工坊与 AI 分身，首页保持无搜索（未提交）
- 将原导航方案 3「L 型」重编号为方案 1 并设为默认，原方案 1 顺延为方案 3，同时迁移既有本地选择（未提交）
- 恢复方案 3「发布作品」按钮描边并改为 1px 内描边，避免被侧栏边界截断（未提交）
- 去掉方案 3 首页白色「发布作品」按钮的描边与悬浮阴影（未提交）
- 将导航方案 3 首页侧栏的「发布作品」主操作改为白色按钮，其他方案保留黑色（未提交）
- 将方案 3 首页顶部视频的纵横遮罩改为白色收口，并同步容器底色消除上下分割线（未提交）
- 统一所有侧栏顶部主操作按钮为 13px，并让图标起点与图文间距对齐下方菜单行（未提交）
- 为导航方案 3 的创作者中心首页右侧内容补齐白色底板，使下半区与页脚完整包裹（未提交）
- 隐藏方案 5 在创作者中心首页的底部工具栏，并从头像菜单移除权限管理/直播管理区（未提交）
- 在百科导航方案 5 的底部工具栏增加「我的词条 23」icon 操作（未提交）
- 按导航结构重命名方案 1/2/4/5 为「底部收起 / 顶部工具栏 / 增加 Header / 底部工具栏」（未提交）
- 为头像菜单中的导航方案补充可读名称，方案 3 命名为「L 型」并改为编号+名称选择（未提交）
- 为首页导航方案 3 的百科和 AI 工坊侧栏补充底部上下文操作「我的词条 23」与「偏好设置」（未提交）
- H5 快速编辑新增对象类型徽标与对象信息区，明确区分组件、图片、卡片、按钮和文本并展示对应元数据（未提交）
- 在 H5 快速编辑关闭按钮左侧增加对象级历史记录，图片展示对应生图版本、模型、Prompt 与恢复入口（未提交）
- 精简 H5 快速编辑头部并对齐 40px 工具条，移除顶部 AI 输入框，将图片 Prompt 下沉到预览图下并补齐文件后缀（未提交）
- 扩展 H5 快速编辑为组件/元素/卡片三级命中，补齐游戏卡、作品卡、图片与按钮的独立选择和对应属性（未提交）
- 将 AI 工坊 H5、小游戏与兴趣卡的「基础信息」入口改为「项目文档」并统一置于菜单首位（未提交）
- 修复素材上传结果与产品目录节点的 TypeScript 类型，解除 Vercel 生产构建阻塞（未提交）
- 快速编辑升级为对象级字段联动：H5 延续楼层/元素映射，兴趣卡与小游戏新增可点选对象及对应属性面板（未提交）
- 恢复 H5、小游戏与兴趣卡的基础信息入口，并在同一详情中保留基础信息/文档切换（未提交）
- 素材库工具条新增上传与 Grid/List/页面使用视图，游戏按开始页/战斗页等逐行归类，并统一画布编辑 icon（未提交）
- 图片浮动工具条取消横向滚动并将次级操作收进更多菜单，左侧入口改为可进入现有素材画布的「画布编辑」（未提交）
- 方案 3 顶部产品 Tab 尺寸对齐方案 1，并将倒 L 导航右侧的对话与预览共享底板统一为纯白（未提交）
- 复用既有图片快捷工具条到素材大图详情，补齐 hover 图片操作、上传替换与兴趣卡素材 Prompt 下钻（未提交）
- 将 H5、小游戏与兴趣卡的素材库统一前置，并把数据库移动到项目文件夹正上方（未提交）
- 按参考图重组 H5、小游戏与兴趣卡目录，保留真实项目文件夹并统一置底（未提交）
- AI 工坊默认进入 H5，并从所有项目产物目录移除基础信息与页面配置节点（未提交）
- 小游戏预览编辑入口对齐 H5 工具栏，项目目录改为 H5→小游戏→兴趣卡，并全局统一 4 条大字号初始推荐指令（未提交）
- 串联抖音 ACG 项目行预览定位、塔罗兴趣卡专属属性联动、全项目素材库命名与游戏沉浸画布入口（未提交）
- 重做「塔罗兴趣卡」为 Figma 原始素材与 DOM 分层渲染，移除整屏截图并在左侧新增可用的 5 项素材库（未提交）
- 新春会 H5 按 Figma 8:11955 抽出 12 项独立活动素材并新增可编辑 Prompt 下钻，同时为游戏 51 个逻辑素材复用同一详情链路与生成元数据（未提交）
- H5 预览工具栏按 Figma「探索」0:49570 还原为 40px 白底弱分隔结构，并替换项目标签、编辑入口顺序与原始矢量图标（未提交）
- H5 沉浸画布将编辑工具栏改为全宽顶部通栏，对话与图层面板统一从工具栏下方起排（未提交）
- H5 画布编辑升级为 Figma 式沉浸三栏模式，进入时隐藏创作者中心顶栏与 AI 工坊项目导航，退出后恢复原导航状态（未提交）
- 将 AI 工坊「塔罗小程序」统一改名为「塔罗兴趣卡」，按 Figma 1:8282 / 0:52140 像素级还原 Feed 兴趣卡与可切换落地页，并压缩定稿素材（未提交）
- H5 画布编辑保留左侧对话并新增对话/图层树切换，接入实例级拖拽、Option 拖拽复制、图层显隐锁定与重排、撤销重做、键盘微调及选中后才出现的坐标属性栏（未提交）
- 将新春会右上角编辑拆分为「快速编辑 / 画布编辑」，新增隐藏工坊侧栏与对话区的沉浸式长页面画布、图层树、缩放和属性联动（未提交）
- 将「抖音 ACG 游戏新春会」编辑器从可拖动遮挡浮层改为预览右侧常驻可调宽编辑栏，并补齐编辑栏关闭与宽度调整的无障碍标签（未提交）
- 隐藏 AI 工坊侧栏与项目数据面板中的「抖音 AI 工坊设计探索」，保留其配置、文件树和预览实现供后续恢复（未提交）
- 将导航方案 1–5 从页面底部悬浮条收进右上头像下拉菜单，保留当前方案状态、持久化与选择后自动收起行为（未提交）
- 新春会预览改用 Figma 节点精确 PNG/SVG 分层渲染并锁定 375×1551 原稿几何，移除游戏条、视频字幕、榜单卡片与会场入口的 CSS 近似实现（未提交）
- 将「六一儿童节活动」整体替换为 Figma 845:24822 的「抖音 ACG 游戏新春会」，同步主视觉、游戏会场、主视频、开年高燃榜单、项目数据与编辑配置（未提交）
- AI 工坊射击小游戏的素材画布由棋盘格背景改为主题自适应点阵，减少格子噪声并统一工坊画布语言（未提交）
- 方案 3 全局底板改为 170deg 的 #F8F8F9→#F1F3F8 渐变，并把 AI 工坊顶部「AI 创作」按钮描边改为内描边以避免贴顶裁切（未提交）
- 方案 3 按 Figma 425:30414/30415 重构为全宽三段顶栏，侧栏首项与右侧内容统一从 48px 起始，并改为透明承接浅灰蓝渐变底板（未提交）
- 方案 5 底部收起入口改用 Master Icon 的双向左箭头，收起后的展开态仍保留原方向状态图标（未提交）
- 方案 2 首页左侧顶部完全移除并禁用手动收起；其余产品工具条统一改为左对齐、固定 12px 间距（未提交）
- 方案 2 顶部产品导航恢复 icon + 文字；首页移除工具条，AI 分身 / 百科 / 随变 / AI 工坊分别按 1、1256、135、1345 显示侧栏工具条（未提交）
- 方案 2 首页工具条的 6 枚 icon 全部改为从 Figma 完整节点导出的独立 16×16 SVG，移除 CSS 拼接与 mask 渲染（未提交）
- 方案 2 首页左侧导航顶部的产品 Logo 替换为 Figma「统一导航」472:58090 的 6 入口工具条，收起态保留原展开入口（未提交）
- 方案 3 移除右侧内容区外侧的顶部与左侧描边；新增完整复用方案 4 的方案 6，并将 AI 分身左上「管理分身」替换为可筛选分身的搜索框（未提交）
- 方案 3 顶部与侧栏包围的内容区统一增加 20px 左上内圆角，使用共享导航底色遮罩且不裁剪页面弹层（未提交）
- 方案 3 顶部导航、左上品牌区与产品侧栏统一为连续导航底板，移除顶部内部接缝并保留 L 型内容边界；随变同步方案 3 专属底色与描边（未提交）
- 随变菜单精确对齐 Figma 425-31142：校准分组操作间距、角色库/世界书列表层级、图标自然尺寸、选中态、底色与分隔线，并由共享侧栏覆盖方案 1–5（未提交）
- AI 工坊右侧顶部默认仅打开「预览」，修正预览图标并移除发布右侧的收起入口（未提交）
- 随变侧栏中段按 Figma 425-31142 更新为任务、我的角色、角色库与世界书，并统一应用方案 1–5（未提交）
- 方案 4 AI 工坊侧栏底部新增「偏好设置」，位置与百科「我的词条」一致（未提交）
- 五套对话输入框统一为 114px，左侧工具栏增加可选择本地文件的「+」，欢迎区居中且快捷指令统一竖排（未提交）
- 方案 4 AI 分身左侧 Header 标题由「开启创作」改为「管理分身」，其他产品与方案保持原文案（未提交）
- 四枚收起展开 SVG 的路径填充统一为 Semi text-1 token（rgba(28,31,35,.8) fallback），移除原 35% 透明度（未提交）
- 四枚面板状态 SVG 源文件保留 24×24，所有界面收起展开入口统一以 16×16 渲染且保留原按钮热区（未提交）
- 从 Figma layout 组件集 400:50283 精确导出四个 24×24 左/右面板状态 icon，并统一替换导航、内容区与预览区的收起展开入口（未提交）
- 按 Figma 统一导航 396:40119 重做方案 4 百科侧栏：40px 对象工具头、无折叠箭头目录层级、精确导出操作 icon 与底部「我的词条 23」（未提交）
- AI 工坊在全部导航方案下首次进入时默认选中并打开「塔罗小程序」，不再先显示新建项目首页（未提交）
- 方案 2 五个产品统一收起为 60px 竖向导航：默认保留产品 Logo，hover / 键盘聚焦时原位切换为展开 icon（未提交）
- 方案 1 五个产品统一使用 SideNav 的 60px icon rail 收起形态：保留菜单图标与底部展开入口，不再将左栏完全隐藏（未提交）
- 方案 3 AI 分身侧栏首项做 6px 光学上移，使「技能库」可见图标与百科、随变等产品的 Logo 下方视觉距离收敛（未提交）
- 按 Figma 统一导航 380:27227 的组件层重建方案 2 五个产品 Logo：统一保留 24×24 画布与原始内部矢量偏移，撤销百科单独缩放补偿（未提交）
- 方案 2 百科左上 Logo 从统一 18px 盒子做 20px 光学补偿，修正横向星芒 SVG 在 contain 后视觉偏小（未提交）
- AI 工坊默认展开「塔罗小程序」，并移除工坊右侧内容 Header 与百科 Header 发布按钮左侧图标（未提交）
- AI 工坊侧栏「项目列表」右侧移除收起全部与查看全部项目操作，仅保留带无障碍标签的搜索入口（未提交）
- 百科目录顶部移除「世界书目录」，接入 Figma 统一导航 416:29915 的对象切换器；支持灵笼、西游记、三体、鹅鸭杀切换并同步编辑上下文（未提交）
- 方案 1 五个产品统一复用底部「收起导航」组件，规范行高、圆角、内边距、图文间距、字号、颜色与交互状态（未提交）
- 统一方案 1 百科底部「收起导航」与其他产品的左侧基线，移除多占一列的隐藏树形箭头槽并复用共享导航尺寸变量（未提交）
- 方案 2 AI 分身与百科的产品 Header 到下方首项间距改为 0，其他产品继续保留 12px（未提交）
- 方案 2 五个产品的左上 logo 统一替换为 Figma「统一导航」节点 380:27227 导出的原始 SVG，并保留矢量渐变（未提交）
- 修正方案 5 百科底部 icon 区多出的 16px 右偏，与首页、工坊和随变统一左侧基线（未提交）
- 新增导航方案 5：沿用方案 1 的顶部与侧栏结构，左侧底部改为 icon-only 收起入口，并新增项目设置 icon 入口（未提交）
- 修正方案 4：顶部产品导航恢复 icon + 文字，「开启创作」右侧继续保留收起按钮，撤回误移到底部的入口（未提交）
- 方案 4「开启创作」Header 统一为贴顶 40px 且与下一项零间距；工坊首页、项目对话和全局 AI 助手输入框移除附件与 Figma 入口（未提交）
- 新增导航方案 4：完整沿用方案 2 的全宽顶栏、纯文字 Tab、间距与收起逻辑，五个产品的左侧 Header 统一用 13px「开启创作」替代 icon/logo（未提交）
- 移除共享左侧导航切换时的横向位移，保留 160ms 轻淡入，消除展开/收起瞬间的晃动感（未提交）
- 左侧导航展开/收起统一加入 160ms ease-out 的位移与透明度动效，兼容 reduced-motion 且不影响拖拽调宽；补齐随变方案 1/2 的真实收起与展开入口（未提交）
- 方案 2 左侧产品 Header 到下方首项统一为 12px：移除 AI 分身与随变各自叠加的第二层 12px 留白（未提交）
- 百科内容 Header 的发布按钮同步 AI 工坊样式：28px 高、12px 字号、紧凑内边距与上传图标，并明确使用 8px 圆角（未提交）
- 方案 3 左上品牌 Logo 从 20px 回调至 22px，在原始 24px 与缩小稿之间取更清晰的折中尺寸（未提交）
- 方案 3 左上品牌 Logo 从 24px 缩至 20px、导航展开/收起图标从 20px 缩至 16px并保留 32px 品牌/状态按钮热区；AI 工坊、AI 分身、百科的对话与内容 Header 全部统一为 40px（未提交）
- AI 工坊、AI 分身与百科统一对话 Header / 内容 Header 的纵向基线：移除工坊右侧 12px 顶部偏移，并把百科对话标题条统一为 60px 高度（未提交）
- AI 分身工作台移除右侧内容容器的 12px 顶部偏移，使对话流 Header、一级标题条及其内容同轴上移对齐；同时隐藏发布按钮右侧的「收起预览」（未提交）
- 方案 3 移除各产品侧栏底部收起行，把 icon-only 收起/展开按钮放到贯通左栏的 Header Logo 右侧；按产品共享收起状态，并为收起后的品牌头保留 60px 展开入口（未提交）
- 方案 1 各产品的「收起导航」图标改为复用菜单行的图标尺寸、内边距与列间距；百科额外复用目录箭头占位槽，使底部图标和文字与菜单列精确对齐（未提交）
- 百科方案 1 的收起入口从目录标题移至左栏底部，统一为「收起导航」并复用本地 on/off 面板状态图标；方案 2 顶部入口保持不变（未提交）
- 方案 1 底部收起导航统一复用本地面板状态图标：展开态 `/icons/on.svg`、收起态 `/icons/off.svg`，覆盖创作者中心、AI 工坊/分身、随变与侧栏规范页（未提交）
- 方案 2 侧栏展开/收起按钮改用本地 `/icons/on.svg` 与 `/icons/off.svg`，并通过 mask 保持浅色与 hover 着色（未提交）
- 方案 2 产品侧栏 Header 视觉减重：产品 icon 从 22px 缩至 18px，收起按钮默认色降至 45% 并保留悬停加深（未提交）
- 导航对比扩为三套并恢复原方案 2：方案 2 继续使用全宽品牌顶栏、纯文字 Tab 与产品侧栏头，贯通左栏的新布局顺延为方案 3（未提交）
- 新增贯通左栏布局（后顺延为方案 3）：沿用方案 1 的菜单与底部收起，把创作者中心 Logo 并入左栏，顶部产品导航仅覆盖右侧内容区且不再重复 Logo（未提交）
- 分身/百科默认视为已开通，进入即编辑器：Shell 摘除 AvatarLandingPage 分支（分身 Tab 首次进入直接挂载分身版工坊），WikiWorkspacePage 去掉 home 视图直进世界书编辑器，WikiSideNav「主页」行仅在传入返回入口时渲染；launch.json 开 autoPort、vite 端口兜底读 PORT（未提交）
- 对话输入框统一高度 120px：新增共享 ChatComposer（卡片壳+textarea+底部工具条，皮肤可配），随变/百科编辑器/工坊首页三处 textarea composer 迁移接入；工坊项目内与创作助手两处 contentEditable composer 同步定高 120px 内部滚动（未提交）
- 百科 Landing 页不再渲染世界书目录侧栏（两个导航方案一致），目录只属于编辑器；顺带移除 WikiHomePage 的展开目录按钮与相关 props（未提交）
- 导航方案 2 推广到全产品：nav-version 改为共享 zustand store；首页/随变/百科/AI工坊/AI分身侧栏顶部统一「产品 logo 占位 icon + 收起按钮」（参考设计稿 统一导航 369-33199），方案 2 下隐藏各自底部收起（未提交）
- 百科世界书编辑页：右侧对话面板通栏贴边（去外边距、白底 + 左分隔线），中间内容区顶栏右侧新增「发布」按钮（未提交）
- 导航收起方案对比测试：新增方案 2（顶栏保留品牌 logo、tab 纯文本；侧栏顶部为产品自有 logo 占位 + 右侧收起按钮，去掉底部收起），底部居中悬浮切换器可在方案 1/2 间实时切换（未提交）
- 创作者中心全局顶部导航改用紧裁的 PNG 品牌 Logo，并保持原有横向占位避免挤压产品导航（未提交）
- 修复页面内导航拖拽时多条竖线叠加：移除工坊 / 分身侧栏宽度与内容偏移的布局过渡，并将拖拽及键盘焦点反馈收敛为同一条边界线（未提交）
- 五个顶层产品的页面内左侧导航统一支持右边缘拖拽调宽，并共享 160–480px 约束、键盘方向键 / Home / End 与无障碍分隔条语义（未提交）
- 百科产品层只挂载一份统一世界书目录侧栏：首页默认高亮主页，目录文档可进入对应编辑页，首页 / 编辑切换时保留分组展开、拖拽宽度与收起状态（未提交）
- Vercel 增加 `/sidebar` → `/index.html` 的精确 SPA rewrite，修复生产环境规范页直链 404，同时保持 `/api/*` Serverless Functions 不受影响（未提交）
- 发布前修复完整 CI 的 4 个 Lint 阻断：组件文件移除非组件导出、作品日期格式化抽到独立模块、渲染期时间改为模块级稳定值、百科标题移除全角空格并保持视觉间距（未提交）
- 所有共享侧栏下拉 / 展开图标统一为设计稿指定的 `chevron-down-small (Arrows)`，覆盖菜单、项目树、百科、随变与发布入口，并移除百科的实心三角特殊分支（未提交）
- 百科世界书目录的展开箭头改为尖角实心三角形，保留共享 Disclosure 的 24px 点击热区、位置配置与展开旋转（未提交）
- 百科 WikiSideNav 接入共享 SideNav 外壳与配色变量，统一底色、分隔线、宽度配置及目录行选中/悬停色，同时保留世界书多分组树和拖拽交互（未提交）
- /sidebar 全局布局样式二改用真实百科世界书目录导航，并让顶部产品导航默认选中「百科」（未提交）
- /sidebar「全局整体布局」新增左侧内容、右侧 440px 对话样式，复用现有侧栏/对话/内容蓝图，并按统一导航参考稿默认省略第二层对象工具条（未提交）
- Sidebar「发布作品」前置图标由 Add01 换成指定的 slide-wide-add（SlideWideAddLinearIcon），规范 Demo、收起态与真实创作者中心同步（未提交）
- 侧栏展开/收起箭头统一为共享 MasterIcon ChevronDown 16px：覆盖 SideNav 子菜单、项目/FileTree、百科、随变及发布作品下拉，保留各场景原点击热区与方向语义（未提交）
- /sidebar「查看 Diff」入口按反馈由文件形 Diff 图标改为方框内加减号的 Changes 图标（未提交）
- /sidebar 全局整体布局的「对象 1」工具栏右侧补充版本记录、查看 Diff、编辑三个图标按钮示范，并完善悬停提示与无障碍名称（未提交）
- SideNav 菜单行间距默认值由 4px 调为 0，并自动迁移旧的 4px 本地存档，使规范页与真实侧栏同步紧凑排列（未提交）
- /sidebar 各组合 Demo 改为独立且互斥的菜单/树选中态，移除项目父行常驻灰底；将两个 13px 字号标注收进主组件并恢复示例卡统一 32px 间距（未提交）
- 展开箭头配置收窄并更名为「树展开箭头位置」，仅控制 FileTree/项目树/世界书等树披露；创作服务等 SideNav 一级菜单固定在右侧，并兼容迁移旧配置字段（未提交）
- 项目列表/我的AI分身到上方菜单间距由 12px 调为 0；我的AI分身由 section Header 改为与塔罗小程序同级的 13px 一级可展开菜单，规范页与真实侧栏同步（未提交）
- /sidebar 组件配置新增「展开箭头位置」左/右切换并支持保存，统一驱动 SideNav 子菜单、项目/FileTree、世界书目录和我的AI分身分组；补枚举清洗与回归测试（未提交）
- 世界书目录标题字号由 14px 统一为与目录条目相同的 13px，并实测项目列表/我的AI分身到上方菜单间距均为 12px（未提交）
- /sidebar 右侧组件配置支持收起，收起后画布铺满并在页面右侧保留悬浮「组件配置」按钮用于恢复（未提交）
- AI 分身左侧栏名称改为读取陶白白分身配置，并将「资源库」到「我的AI分身」间距从 16px 对齐统一 SideNav 规范的 12px（未提交）
- SideNav review 收尾：修复子菜单深链同步、短视口滚动、收起焦点隔离与无障碍标签，统一全局宽度/主题/内边距，文件树补目录选中、24px 披露热区及完整路径去重；配置新增边界清洗、保存错误反馈、键盘编辑与回归测试（未提交）
- /sidebar 右侧组件配置输入框去掉与自身边框叠加的全局 2px 焦点环，数字、透明度与取色输入统一为单层 Semi focus 蓝色边框（未提交）
- SideNav 收起态总宽 61→60px，不再为右侧 1px 分隔线额外增宽；旧版默认 61 存档自动迁移，分隔线计入总宽（未提交）
- FileTreeView 文件夹图标按确认名称调整为收起 Folder2LinearIcon、展开 FolderOpenFrontLinearIcon，均不带附加箭头（未提交）
- FileTreeView 文件夹两态换成 MasterIcon 无附加箭头版本：收起 Folder2LinearIcon、展开 Folder02LinearIcon；左侧披露箭头继续只负责树展开（未提交）
- /sidebar 去掉主组件 Skills 前方的绿色 12px 左右内边距标注，保留真实内边距与右侧配置项，减少标注遮挡（未提交）
- 导航默认底色统一为 `background: var(--semi-color-nav-bg, #F2F2F7)`：共享 SideNav、世界书、随变及智能体广场导航全部接入，并迁移旧版默认白色存档（未提交）
- /sidebar 主组件宽度标尺从顶部移到组件下方，使其上沿与右侧场景卡对齐；移除独立的底部规格、选中状态与四级分类说明，只保留组件内标注（修改前已创建本地备份，未提交）
- /sidebar 配置区由悬浮抽屉改为参与横向布局的常驻右栏，固定占用 384px 并挤压主画布；暂时移除展开收起入口与动画（未提交）
- /sidebar 将顶部组件配置改为右侧可收起抽屉：默认仅露出 44px 配置标签，展开后双列滚动编辑，保存/重置固定底栏且不改变画布几何（未提交）
- 修复 /sidebar 规范画布因全局布局示例缺少导航常量与状态而运行时空白，补齐蓝图菜单、收藏项及受控选中态（未提交）
- 模块图标语义对齐:基础信息 File01→InformationCircle、能力配置/技能/技能库/能力技能 FolderCode→MagicWand01、智能体 BubbleChat→Bot(文件夹已是 Folder02/FolderOpen);画布卡片标题下说明小字全部移除(标尺补 mt-10 头顶空间);/sidebar 新增「全局整体布局」蓝图(按统一导航 321-41651,1440×900:顶栏 48/侧栏 220/对话列 440(Header 40·Composer 120·气泡 42/字 14)/右侧自适应(双工具栏 40·内边距 16),0.6 缩放标真实值);修 HMeasure 高度为 0 时两条虚线 key 冲突(未提交)
- 配置新增「侧栏底色」(bg,默认 #ffffff,注入 --sidenav-bg),配色组置顶,同样走取色器+透明度(未提交)
- 树箭头槽默认 14→16(与图标同宽),每级缩进随之 18→20(=槽 16+间距 4,子级箭头仍对齐父级图标);面板「列间距」改名「元素间距」(即行内 箭头槽↔图标↔文字 之间的 4px);修随变「我的世界」分组图标拉伸(earth.svg 16×12 被 size-4 拉成 16×16,加 object-contain 保比例)(未提交)
- 侧栏四周间距统一 12:上内边距默认 16→12,主按钮外壳去掉 pt-1(按钮距顶正好 12),底部收起导航 pb-4→pb-3(距底 12),左右本就 12;首页/工坊/随变挂载处同步,标注几何推导跟随更新(实测上/左/右/下全为 12,标注仍对齐)(未提交)
- 标注与配置联动:标注位置全部改由配置推导(g.menuRow/treeRow/图标列等,不再写死坐标),改配置时标注跟着组件一起动(实测行高 36→44 后标注仍与真实行对齐);所有读数(chip/字号框/顶部宽度标尺)带 editKey 可点击就地输入直改配置;lab 项目行的行高/左边距/字号也接入配置(未提交)
- 配置面板迭代:配色改原生取色器+透明度%(hex⇄rgba 双向解析),数值项改单列不再截断 label;画布卡片里组件自身右描边关掉(与卡片 ring 叠成两层);默认值统一:主按钮圆角 10→8(同菜单行)、字号 14→13(全组件 13);百科世界书卡挪到随变前(未提交)
- SideNav/树的属性抽成运行时配置(新增 side-nav-config.ts:zustand store,24 项尺寸+5 项配色):SideNav 把配置注入为 --sn-* 变量、FileTreeView/主按钮/图标直接读 store;/sidebar 右侧新增配置面板,修改实时同步所有挂载点,「保存」写 localStorage 全应用生效(验证:改行高 44→保存→首页真实菜单行 44),「重置」回默认;画布规格表读数跟随配置实时变(未提交)
- 标注去掉长引线(间距 chip 直接压在被量的那段上)、字号统一 13px(树行 12.5→13、项目行 12→13,与菜单行一致);文件夹图标换 Folder02/FolderOpen;工坊菜单「创意广场」改名「项目库」(icon 换 FolderLibrary);世界书目录抽成 WikiSideNav 导出并加进规范画布,树几何改走 FileTreeView 共享原语(箭头槽 14/缩进 18/行高 28)(未提交)
- 树图标 14→16(彩色底板 h-4 w-4 内芯 10、文件夹 16、文件 14 装 16 格),文字列相应 74/92→76/94;规范画布高度标注加竖向尺寸线(上下虚线一眼配对),间距/缩进 chip 全部移到组件底部空白区、虚线从色带引下(未提交)
- 树缩进按设计稿 249-18701 重定:每级缩进 18=箭头槽 14+间距 4,图标 14+间距 4 到文字,树基础左边距 8;项目行=箭头+名称(箭头列 20/文字列 44),实测三级列坐标 20/44→38/56/74→56/74/92 与设计稿逐像素一致;规范画布标注改参考稿画法(黑 chip+贯穿虚线量高度、绿/粉色带量间距与缩进、虚线框量图标、白框+引线标字号)(未提交)
- 规范画布标注只留数字,去掉「菜单行/图标/缩进」等说明文字(量的是什么由色块指出,说明留在下方规格表);路由 /sidenav-lab 改为 /sidebar(未提交)
- 修树的层级缩进:项目行去掉文件夹图标后没保留图标那一列,导致项目→模块的文字一次跳 36px(其余层级 16px),看起来第二层缩进过多;补回 20px 的空图标列(置顶项目正好放 pin),现在箭头列 24/40/56、文字列 60/76/92 每级都是 +16;顺带把文件图标装进 14px 的定宽格,同级文件与文件夹的文字不再差 2px
- 规范画布:展示高度 768→600;标注去掉底色胶囊改纯文字,并按量的位置就近摆放(图标/间距/缩进的读数跟在该行文字后面,不再全甩到右边),新增「图标 16」「图标→文字 8」两项(未提交)
- 规范画布展示高度统一 572→768,收敛成一个 CANVAS_H 常量(主组件与场景卡都走它,不再各写一处);随变侧栏抽成 SuibianSideNav 导出并加进画布(自带展开态与拖拽,页面与画布同一份,避免复制后漂移);顺带清掉 VibeCodingPage 里失效的 Folder01LinearIcon 引入(未提交)
- 目录树对齐 macOS Finder 收尾:箭头槽与每级缩进合并为同一个常量 16(此前 14/20 不等值,子级箭头永远落不到父级图标上,这才是错位的根因);项目行去掉文件夹图标只留箭头+名称;「项目文件」不再用分类图标+彩色底板,回落成文件夹图标(展开/收起两态),与内部目录一致;规范画布标注胶囊改贴组件右缘(不再占用右侧标注栏),主组件列宽收到 248px,与场景卡的间距 174→60(未提交)
- 目录树完全对齐 macOS Finder:行高 26→28;项目行也加披露箭头;规范页主组件标注从 9 个精简到 6 个(去掉重复的行高/项目行高)+ 规格表 7 行并为 5 行(未提交)

## 2026-07-26

- 收起态改 36×36 正方形(SIDE_NAV_COLLAPSED_WIDTH=61=36+12*2+1px描边),主按钮收起时也收成方形;「项目文件」由 file 改为 dir 携带真实源码树,FileTreeView 新增 canExpandDir 逐节点区分「模块(只选中)/文件夹(只展开)」,点文件夹只展开不再开页签(对齐 macOS Finder),规范页同步(未提交)

- 规范页标注单位统一:原来混着 Tailwind 简写(pt 16/px 12/gap 4/h 26/pl 12)容易被误读成 pt·px 单位,全改为中文全称+px(上内边距 16px / 左右内边距 12px / 行间距 4px / 行高 26px / 每级缩进 16px 等),规格表同步;导航组件展示高度 430→572px(未提交)

- 收起导航图标改用设计稿指定的 layout-left(LayoutLeftLinearIcon,原 PanelLeftClose 不对),首页/工坊/随变/规范页四处统一;规范页项目行补上展开收起(与线上工坊一致,图标随开合切换),两棵文件夹树的 expanded 初值 key 修正(parentPath 不同导致默认没展开)(未提交)

- /sidenav-lab 规范画布重做:标注改 F12 风格(元素上铺高亮盒 content蓝/padding绿/gap橙 + 右侧窄槽尺寸胶囊,坐标实测自组件),主组件换成 AI 工坊(菜单+树俱全),工坊与分身场景卡内嵌真实产物树,四个场景与主组件并排;新增 FileTreeView 画布(文件夹树标注 + 模块/文件夹对照 + 四级分类说明)(未提交)

## 2026-07-25

- 收敛 SideNav 各产品间的不一致:①SideNavActionButton 两个变体几何统一(h40/r10/14px-600/px16),原 light 是 h36+全圆角+13px;②工坊不再覆写 --sidenav-*(此前未选中项 60% 比首页 80% 淡一档);③工坊补上底部「收起导航」(新增 onCollapseSidebar prop,此前 sidebarCollapsed 只有展开没有收起入口);④/sidenav-lab 重做为单画布:左侧主组件带尺寸标注(宽度标尺+量度括号+引出线)与色值表,右侧平铺创作者中心/AI 工坊/AI 分身/收起态四个场景(未提交)

- 修随变两个问题:①对话栏底色由 #fbfbfc 改为设计稿取样的 #f0f0f1(白输入框/白气泡原本在近白底上看不出边界,输入框像坏了),画布底同步改 #f3f3f4,输入框高度补到 114px;②侧栏滚动条不可见 — .thin-scroll 的 thumb 是为暗色工坊设计的白色,新增 .thin-scroll-light(深色 thumb)供浅色面板使用,随变侧栏与消息区改用它(未提交)

- 随变侧栏按新稿 259-32949 与统一导航对齐:去掉 logo 头与面板按钮、三组(我的项目/我的角色/我的世界)间加分隔线、新增「我的世界」分组(星禾渡·余华《兄弟》,带风格标签+地球/世界图标)、底部头像与星光胶囊换成与首页·工坊同款的「收起导航」(未提交)

- 侧栏宽度统一到 SIDE_NAV_WIDTH(220px):随变由 300px 收窄;随变与百科编辑页补上右边缘拖拽把手(200~360px,与 AI 工坊同一套 pointer 交互)(未提交)

- 随变页三栏去掉外间距与圆角:外层 gap-3px/p-3px 移除、三栏 rounded-2xl 去掉,改为细分隔线贴边铺满(未提交)

- 随变页按设计稿 统一导航 244-19030 实现(替换占位页):新建 SuibianPage 三栏 — 左侧我的项目/我的角色(可折叠)+星光余额、中间创作总监对话(设计稿全部消息+橙色高亮词+工具回执 chip+黄色发送键)、右侧点阵画布(设计导出整图+缩放 HUD+画布模式);素材存 public/assets/suibian,已 sips 降采样+TinyPNG 压缩(49M→996K)(未提交)

- 创作服务图标由客服耳机换成闪电 LightningLinearIcon(语义是创作便捷工具,与统一导航稿 lightning-02 一致);AI 工坊左侧图标全部换 MasterIcon:产物树模块图标 18 个、项目行文件夹/置顶、工具条搜索/展开全部、FileTreeView 默认文件夹、分身侧栏五项(未提交)

- 按四级分类(项目/模块/文件夹/文件)收敛目录:工坊产物树 showDirChildren=false — 模块(能力配置/页面配置/知识库…)只「选中查看」不再左侧展开,子对象走右侧目录下拉;资源库分类树改常驻披露箭头(原 hover 才出现)并去掉子级左侧连接线;左侧导航 AI 创作恢复白底(发布作品保持黑底)(未提交)

- 分身模块名与侧栏入口对齐:新增 AVATAR_SKILL_LABEL「技能库」/AVATAR_TRIGGER_LABEL「触发器」(原复用小程序的 能力配置/触发器配置),产物树·默认页签·分类路由·右栏工具条同步;「代码文件」全局改名为「项目文件」(四级分类里的 文件夹 层)(未提交)

- AI 分身侧栏按截图精简:顶部菜单去掉评测库;「我的AI分身」下改为平铺五项 Ailee(圆头像,进分身预览)/技能库→能力配置/工具库(占位)/知识库/触发器→触发器配置,彩色角标沿用产品树配色,不再渲染项目行与产物树(未提交)

- 目录树统一改 macOS Finder 风格:移除左侧连接线(FileTreeView renderRails/railStartDepth 删除),改为披露箭头(点箭头只展开/收起)+16px 缩进+叶子留空槽保持图标对齐;工坊/分身项目树与百科目录树同步(未提交)
- 百科新增世界书编辑页 WikiEditorPage(设计稿 统一导航 259-32672):左目录树/中文档编辑区(页签+撤销重做历史+宋体标题+来源+面包屑+正文)/右世界书智能体(空态+快速开始三选项+输入框含冲突检测·素材提炼);图标取设计稿导出存 public/icons/wiki-editor;首页「创建世界书」与世界书卡片进入,目录「主页」返回(未提交)

- AI 工坊/分身侧栏样式与创作者中心首页统一:chrome 改 panel(#f2f2f7 灰底+右描边),AI 创作按钮换黑底 dark 变体(彩虹光晕移除)(未提交)

- 内容管理/数据中心/收入变现 三页去卡片圆角、整页白底铺满;内容管理按统一导航稿 275-22737 重构:页面 8px 内边距+头部 16px、页签灰块样式、筛选行 14px 控件+底描边、作品卡 p-4 rounded-lg(审核中灰底)、封面角标/指标/状态色(#3eb346/#ff851d/#fe3824)按稿(未提交)

- 头像下拉菜单图标换成设计稿导出 SVG(public/icons/account-menu,新组件 FigmaGlyph 按设计 inset 精确摆放 + mask 着色);创作主页顶部信息字段按 788-20834 重排:昵称+灰底三角chip、同行 抖音号|签名,数据行 16px 加粗数值 + 12px 右箭头,移除 发起授权/MCN/音乐人徽章(未提交)

## 2026-07-24

- 按设计稿(创作者中心26.7 788-22480/788-20755)新增两处下拉:昵称旁三角出账号切换面板(新组件 AccountSwitcher.tsx,选中黑勾/企业账号Tag);顶栏头像菜单重做为 身份认证/通知中心(红标12)/切换账号(hover 出二级账号面板)/退出登录,直播管理权限开关保留在底部(未提交)

- 按 Figma 统一导航稿(275-22603)重做创作者中心:SideNav 统一 220px/#f2f2f7 底/16px 图标/新 active 色,SideNavActionButton 新增 dark 变体(黑底发布作品),首项改「首页」;创作主页恢复 16px 留白 + rounded-[20px] 白卡(0.5px black/10 描边 + 浅投影),右栏 292px;其余内容页恢复原留白与圆角(未提交)

- 创作者中心右侧内容 section 圆角全部去掉;创作主页改白底、上下两区左右对齐(下区 px-4/lg:pr-2 移除)、section 用 border-black/5 描边区分(HomeSections/数据中心各卡片同步)(未提交)

- 创作者中心首页各页面右侧内容区去掉外层留白贴边铺满:创作主页/数据中心(CreatorCenterHome)、内容管理/收入变现/直播管理/作品共创/活动管理/原创保护/抖音指数/发布作品页外层 px/py 均移除(未提交)

- 新增 SideNav 组件独立调试页 /sidenav-lab(src/dev/SideNavLab.tsx):精简为创作者中心/AI 工坊/AI 分身 3 个真实场景,App.tsx 按 pathname 直出(未提交)

- 右侧全部页面去掉四周留白与圆角改为贴边铺满:首页、资源库、Skills、创意广场、评测库占位页、数据运营、项目工作区(卡片背景框/聊天栏/预览面板同步调整)(未提交)
- 首页对齐 AI 工坊轮廓框架（设计稿 统一导航 250-37291）：侧栏改透明无边框、顶部「发布作品」下拉（四种发布入口）、底部「收起导航」手动收起为 icon rail（SideNav 新增 collapsed/footer 能力）；首页创作助手默认收起为悬浮球（AiAssistantPanel 新增 defaultOpen）(未提交)
- 导航 icon 去透明度：SideNav 图标改走 --sidenav-icon 实色变量（不再跟随行文字的半透明色），TopNav 未激活产品图标固定 #161823 实色(未提交)
- 引入 MasterIcon 图标库（npm install github:li-yingjie/MasterIcon），左侧导航菜单图标统一改为该库 linear 风格；因库 dist/react/index.js 残留 `export type` 导致 Vite 预构建报错，改走 `master-icon/react/<Icon>` 按文件子路径引入绕过(未提交)
- 去掉页面切换的自下而上位移动画：外壳 ProductSurface、创作者中心内容区、百科/占位页/分身落地页、工坊根框架与 Skills/资源库切换全部改为纯淡入(未提交)
- 左侧导航抽象为共享组件 shared/components/SideNav（统一宽度 SIDE_NAV_WIDTH=200 与菜单视觉，--sidenav-* 变量可主题化；支持子菜单/窄屏 icon rail/header+children 插槽）；创作者中心 SideNav 与 AI 工坊 PlatformSidebar（含分身变体）接入，各自菜单内容不变，工坊侧栏默认宽度 176→200(未提交)
- AI 工坊首页按 Figma「统一导航」229:15581 重写 PlatformHome：ASCII 地图纹理底、新版输入框（+/兴趣卡/图片/视频/调研/扩展/Auto 工具条）、兴趣卡介绍 banner（CREATE/SEARCH 双栏）、4 张玻璃手机兴趣卡（hover 预填 prompt、点击直接发送）、精选项目 12 卡网格；素材转 webp 至 public/assets/workshop，保留 draft/onSubmit 交互契约(未提交)
- 百科首页按 Figma「创中版本-世界书」实现：Hero 虹彩背景+宋体标题、世界书/角色档案入口卡（书封扇形贴片、黑胶囊 CTA）、社区权益四栏、发现精彩世界分类筛选+世界书/角色档案切换+12 张世界书卡片；素材导出为 webp/svg 至 public/assets/wiki 与 public/icons/wiki，路由从占位页换成 WikiHomePage(未提交)

## 2026-07-20
- 统一创作者中心右侧助手与 AI 工坊项目 Chat 输入区：相同边距/无 focus 视觉态/工具按钮/占位文案/空内容发送状态，并补中文输入法防误发(未提交)
- 按反馈移除 AI 工坊首页输入框的 focus 视觉态，聚焦前后保持同一张圆角卡片样式(未提交)
- 修复 AI 工坊首页输入框聚焦态：移除 textarea 的方形原生描边，将柔和焦点环迁移到整张圆角输入卡片并保留键盘可见性(未提交)
- 首页、AI 工坊与创作者中心的聊天空态欢迎标题统一为「嗨，我是你的创作助手」，并移除默认副文案里的重复自我介绍(未提交)
- AI 工坊目录按“配置对象/资源产物”重构：小程序统一为基础信息→能力配置→页面配置→数据配置→代码文件，分身补人设/能力/代码，游戏补玩法/数据，H5补页面配置(未提交)
- AI 工坊产物目录术语统一为「基础信息 / 页面配置 / 触发器配置」，并同步图标、标签路由、游戏页面联动与触发器直达逻辑(未提交)
- 首页创作助手产品路由升级为服务端 Kimi 语义分类，按最终创作目标识别 AI 分身/百科/随变/AI 工坊/普通对话，不再依赖字符命中；新增受限枚举接口、等待反馈与安全回归测试(未提交)
- 首页创作助手路由收窄为仅 AI 分身，按实体、操作诉求和间接语义识别并排除资讯/否定/应用分身等误触；分身「去开通」状态写入 localStorage，刷新和再次进入仍保持已开通(未提交)
- 首页右侧创作助手新增产品意图路由：创建 AI 分身跳转 AI 分身，提及小程序/网站/网页游戏/H5 等产物跳转 AI 工坊；产品页用 180ms 淡入位移过渡、尊重减少动态效果设置，隐藏首页同步暂停背景视频(未提交)
- AI 分身侧栏移除「团队空间/我的空间」，并在分身首屏默认展开「我的AI分身」下的项目产物目录(未提交)
- 创作者中心首页左侧栏移除「发布」按钮，并将导航「首页」更名为「创作主页」(未提交)
- AI 工坊项目树合并「基础信息 / 文档」为「项目信息」单入口，右侧用基础信息与文档双视图切换，减少重复层级并保留原有编辑能力(未提交)

## 2026-07-18
- 创作助手展开即聚焦输入框:open 变 true 后 focus inputRef,composer 卡片 focus-within 显示 1.5px 柔和深灰(rgba(22,24,35,0.35))描边(未提交)
- 创作助手悬浮球改白底:蓝渐变→纯白+中性阴影+细边框,logo 去 white 反色用原色(未提交)
- 首页 ASCII 视频顶部遮罩加大:顶部遮罩段从 0-30% 扩到 0-42%、初始不透明度 0.4→0.5,与顶栏过渡更明显(未提交)
- 创作助手悬浮球接入 Lottie 旋转圆圈动画:从 DYAI 仓库取 public/assets/logo2.json(rotating_circles_lottie_loop)+logo2.svg,装 lottie-react/lottie-web(npmmirror),新建 Logo2Lottie(用 lottie-web loadAnimation+dynamic import 绕开 lottie-react 在 vite 下的 default 导出 interop 报错),悬浮球 hover 播放旋转、移开 goToAndStop,white 反色(未提交)
- 百科/随变即将上线占位页图标统一:PlaceholderPage 图标外加统一圆角浅底框(ring+渐变底),消除插画/照片调性差异,两页视觉一致(未提交)
- 内容管理作品类型从标题前 chip 改为封面左上角蓝色 Tag(列表 WorkRow + 详情 WorkDetailPage 头卡一致,标题恢复顶格);首页内容顶部 pt-12→pt-4,四周间距统一 16(未提交)
- 创作助手改名(AI 助手→创作助手,头部/空态/aria 全改);展开收起加动效(AnimatePresence+宽度动画,悬浮球 scale 淡入,拖拽时抑制过渡);配色去紫改纯蓝(悬浮球 #5B9DFF→#3370FF,拖拽高亮 #3370FF)(未提交)
- AI 助手面板左边缘可拖拽调宽(300~560px);首页 ASCII 视频改固定高度裁剪容器+遮罩内置,修复缩放后底部彩色边漏出,顶部加淡入过渡遮罩、整体加深(未提交)

## 2026-07-17
- AI 助手输入框去掉附件/Figma 按钮,左侧新增圆形 + 号;首页内容四周间距 px-6/pb-6 收到 px-4/pb-4(16px)(未提交)
- 内容管理接入 AI 工坊作品:WorkItem 加 workshopKind,ContentPage 注入工坊作品(小程序/AI分身/网页游戏/营销H5)并混入列表、体裁筛选扩展、作品计数含工坊数;WorkRow 工坊口径指标+类型角标/chip;WorkDetailPage 工坊作品头卡改「来源/类型」、隐藏播放键、总览 tile 换访问/访客/转化口径(未提交)
- AI 助手头部去掉 logo,收起 icon 改 double-arrow-right(ChevronsRight);收起态从窄 icon 栏改为右下角渐变悬浮球,球内图标由 Sparkles 换成与空态一致的 LogoIcon 圆环点阵(未提交)
- 内容管理作品行整行可点进详情(hover 整行浅底+标题下划线,键盘可达);行内操作/评论格 stopPropagation 避免误触(未提交)
- 内容管理页对齐设计稿 26.7(20-8421/20-9666):工具栏重排(页签+状态文字筛选+时间/体裁/搜索/导出/创建合集),作品行去迷你图换设计稿指标条,新增作品合集列表;新增 WorkDetailPage(1-38221/1-38571)总览双卡图表+评论管理,列表点封面/标题/评论进详情,支持前后作品切换(未提交)
- 首页外层移除靠 AI 助手一侧的滚动条占位，让两栏无缝相接；恢复首页卡片右侧 24px 内边距以保留内容呼吸感(未提交)
- 首页 ASCII 背景视频底部遮罩取消跟随视频缩放，并拉长淡出区至页面底色，消除与数据概览之间的硬分隔(未提交)
- 小游戏「页面」接入与小程序一致的可视化路由：开始/对局/结算三项改用游戏画面与结算视觉稿，不再展示纯文字信息视图(未提交)
- 删除 AI 工坊项目预览右上角「运营数据」入口及其抽屉挂载，保留独立运营数据页面能力(未提交)
- 分身侧栏对齐设计稿 661-99150:去掉 AI 创作按钮,导航换为技能库/资源库/评测库/团队空间/我的空间(后三者为占位页),项目分组改可折叠「我的AI分身」,分身项目行用圆形头像;去开通 toast 移除(未提交)
- 目录树关键节点上彩色图标底板(参考 Figma WoW-26 661-99330):FileTreeView 新增 badgeFor,PRODUCT_CATEGORY_BADGES 色板,产品树一级节点彩色/子级单色,行样式微调(12.5px/圆角8),AI 分身与工坊共用(未提交)
- AI 分身开通后进入分身版工坊:VibeCodingPage 新增 variant(workshop 隐藏陶白白/avatar 只保留并直进该项目),落地页「去开通」切换,外壳双实例 keep-alive(未提交)
- 创作服务菜单默认收起(serviceOpen 默认 false)(未提交)
- 直播管理菜单默认隐藏(live-store enabled 默认改 false,头像菜单开关仍可开启)(未提交)
- AI 助手去掉界面上的机制自述:头部页面标签 chip 移除、占位符改通用、欢迎语改自然文案(Tab 联动只体现在建议内容里)(未提交)
- AI 助手面板与工坊聊天栏统一视觉件:ChatEmptyState 抽到 shared 双端共用,composer 复刻工坊同款(圆角24白卡+彩虹光晕+扩展/附件/Figma/Auto/发送),index.css 新增 .light-scope 让浅色变量可作用于子树(未提交)

## 2026-07-16
- 新增系统级 AI 助手:LiveAiReply 抽到 shared(VibeCodingPage 改为引用),新建 AiAssistantPanel 常驻创作者中心首页右侧,语境/建议/对话线程随左侧导航 Tab 切换(assistant-contexts.ts)(未提交)
- 修复 AI 工坊聊天栏与预览区重叠:chat aside 从带 margin-left 的 body 容器移出为根容器直接子元素(fixed→absolute 批量改造时定位父级选错,left 被双重偏移)(未提交)
- launch.json 通过 VITE_DEV_PORT=5180 固定 dev 端口(5173 被其他项目占用,vite 只落到 IPv6 导致预览黑屏)(未提交)

- 修正创作者中心作品发布层级：四类入口先展示上传/新建网关，点击后再下钻到对应编辑与发布设置页，文章补齐新建/导入双入口。(未提交)
- 创作者中心作品发布入口补齐四类链路：发布视频/图文/全景视频/文章均进入统一发布页，顶部对齐抖音上传页 tab 与拖拽上传卡，并为非视频类型补充对应表单和发布设置。(未提交)

## 2026-07-14

- 拆分首屏/二级页面与图表按需 chunk，Garuda 动画改为核心帧启动+技能/敌机按需加载；移除头像第三方人设泄露，修复 AI 工坊会话栏覆盖顶导航，并新增 7 项安全回归测试与 GitHub Actions CI。(未提交)
- 修复 AI 工坊新项目批处理串线，按项目/会话保存完整对话快照并使用稳定消息 ID 隔离 AI 缓存；发布流改为按项目存储，SSE 缺少 `[DONE]` 时失败且不缓存。(未提交)
- 加固 Kimi 代理（同源/消息/模型白名单校验、请求与输出上限、超时、并发和频率限制），统一 API 的 JSON 404/405，增加生产静态缓存与压缩并按上海时区跨日刷新数据；排行榜改为服务端 ID、不可覆盖写入、成绩边界与可配置 CORS，同时移除生产 Figma capture 脚本。(未提交)
- 修复创作者中心构建阻断、窄屏顶栏/侧栏子菜单/首页卡片溢出，并补齐筛选、删除撤销、发布对话框、单选/开关/页签的键盘与焦点语义；背景视频按可见性和减少动态效果偏好暂停。(未提交)

## 2026-07-06

- 创作者用户头像替换为现有 `/assets/kingjaylee.PNG`，首页资料区、顶栏头像和账号弹层通过 `CREATOR_PROFILE` 同步更新。(未提交)
- 首页背景视频切换为完整 `ascii-animation2.mp4`，采用 `object-bottom` 保留 ASCII 天空与山丘内容；移除覆盖全画面的重遮罩，仅在底部 55% 区域渐进过渡到 `#F5F6F8`。(未提交)
- 修正入口后卡 hover 轴心/方向：默认 10°+斜切保留在内层，hover 外层改为以后卡左下角为圆心向右增加 4°，并用 180ms ease-out 反馈，避免绕左上角继续下坠。(未提交)
- 按 Figma 节点 1:24030 精确还原入口叠卡默认几何：容器 77×84，前卡 60×75@`(0,5.1)`，后卡 60×75@`(17.94,0)`、旋转 10°并横向斜切 -1.54°；容器上移 5.1px 保持前卡与 75px 入口卡上下对齐。(未提交)
- 入口后方第二张卡按确认值改为默认旋转 10°、hover 13°，保持当前左移位置。(未提交)
- 入口后方第二张卡默认旋转角 3°→5°、hover 9°→10°，同时左偏移 10%→6%，呈现更明显的斜置并更靠左。(未提交)
- 智能创作/作品发布入口左侧正卡改为 75px 高并按高度缩放，与入口卡整体高度严格一致，不再向上悬出。(未提交)
- 入口后卡继续收敛：默认/hover 旋转角降至 3°/9°，左偏移 14%→10%、宽度 83%→82%，减少右下角露出。(未提交)
- 智能创作/作品发布入口的共用后卡默认旋转角由 9° 降至 6°，减弱静止态倾斜，保留 hover 扇开角度。(未提交)
- 首页布局调整为数据概览整行置顶，下方双栏让活动中心与互动管理顶端同排（收入变现/快速导航分别续接左右栏）；`lg` 右栏 300px、`xl` 恢复 336px，互动管理内部横排延后到 `xl` 防止窄主栏溢出。(未提交)
- 首页板块改名/跳转/自适应修复:**① 变现中心→收入变现**(标题与注释同步);**② 查看更多跳转** —
  给 HomeSections 各板块加 `onMore` 回调,首页所有「查看更多/查看详情」接上对应页:数据概览→数据中心、
  互动管理→内容管理、收入变现→收入变现(income)、活动中心→活动管理(service:活动管理)、
  快速导航→创作服务(service,暂为占位页,无独立工具页,待确认目标);**③ 收入变现内容溢出** — 视口
  ~1280px 时正好越过 `xl` 断点,金额+两张任务卡强行并排(201+630 > 左栏 638)横向溢出;把外层
  `xl:flex-row` 提到 `2xl:`,并给卡片容器/任务卡补 `min-w-0`,窄栏改为竖排(金额在上、两卡并排),不再溢出。
  预览实测:5 个入口跳转正确、收入变现无溢出、无报错。(未提交)
- 按设计稿(947-41110)补齐首页图标后卡 + 修顶部视频黑边:**① 后卡** — 上一版只有百科/随变有后卡,
  其余(AI分身/工坊/4 个作品发布)丢了后卡;改为 `CardImageIcon` 里**每张卡都有与正卡等大的斜置后卡**
  (右后方探出、hover 再扇开):有图的铺图(AI分身橙色人像 / 百科耳机盒 / 随变情侣,统一用未旋转原图由
  CSS 做圆角+9°旋转),没图的用中性浅色底板(工坊 + 4 个作品发布,对应设计的白底后卡)。**② 顶部黑边** —
  ASCII 视频四周本身有暗角(vignette),透过遮罩在顶部/两侧显出黑影;用 ffmpeg 把视频裁成干净的天空带
  (`crop=1220:320`,去掉暗角与草地),CSS 回退为普通 object-cover。视频 54KB→20KB;新后卡图 TinyPNG 压缩。
  预览实测:8 张卡均有等大后卡、顶部无黑边、视频自动播放。(未提交)

## 2026-07-05

- 首页图标改用设计师导出的 4x 贴纸图 + 顶部加 ASCII 天空视频:**① 换图** — 用户在 `public/bg`
  导出了 8 张 4x 卡片(4 作品发布贴纸 + 4 智能创作正卡)及百科/随变的斜置后卡;统一为
  `CardImageIcon`(正卡在左、可选后卡右后方探出 + hover 扇开),删掉上一版 CSS 重建的图标块与
  raw 图层合成件;图片重命名为 ASCII 并入 `icons/creator-center/`(publish-*、entry-*),
  TinyPNG 压缩 766K→223K,清掉 `bg` 里的中文名重复件。**② 顶部视频** — 顶部天空底色其实是
  `bg/ascii-animation.mp4`,改为 `<video>` 背景(object-top、300px)+ 淡蓝渐变遮罩向下淡出到
  页面底色(文字可读);视频用 ffmpeg 降到 1280 宽重编码 **8MB→54KB**。改 data.ts +
  CreatorCenterHome。预览实测视频自动播放、图标清晰无 404。(未提交)
- 首页「智能创作 / 作品发布」图标对齐设计稿(Figma node 947-41205):**① 修复图标发虚** —
  原 8 张前/后卡 PNG 均为 1x(60×75),2x 屏必糊;改用 Figma 高清原图层重建,前卡=话题正图、
  后卡=潮流单品叠放贴纸(AI工坊特殊:手持手机 hand 叠加应用截图 screen)。**② 作品发布图标改样式** —
  由原「纯色饱和方块+白描边字形」改为设计稿的分层贴纸:浅色渐变底卡 + 居中高饱和圆角图标块
  (含内阴影/白色字形)+ 背后中性叠层;全景视频用同构 VR 眼镜 SVG。新增 8 张 entry-*-front/back/
  hand/screen 图(TinyPNG 压缩 1188K→340K),删除旧的 entry-*.png/-front/-back;改 data.ts
  条目结构 + CreatorCenterHome 的 SmartCreateIcon/PublishTile。预览实测无报错、图标清晰。(未提交)
- 新增发布视频表单页 PublishVideoPage.tsx(高保真还原抖音「发布视频」内容区:基础信息/
  设置封面/添加合集/自主声明/扩展信息/发布设置 + 右侧上传进度面板,纯占位数据、CSS 占位图),
  接入 CreatorCenterHome 左栏「发布」按钮与首页「发布高清视频」卡片(page='publish-video')。(未提交)

## 2026-07-04

- 【事故恢复】备份 commit de80b2b 之后的未提交改动被 21:23 的 git reset --hard +
  git clean 清空，已全部重建：直播管理折线图真实化（12点钟形+指标自洽）、首页
  home-overview 扩展（interaction/monetization/calendar/quickNav）+ HomeSections
  （互动管理/变现中心/活动中心日历/快速导航/页脚）+ 双栏布局、入口卡贴纸构图
  （EntryCard/StickerIcon/SmartCreateIcon/PublishTile，8 张分层 PNG 从 Figma 重下）、
  扇牌 hover（后卡自身左下角为圆心纯旋转 4°、spring 回弹）、左侧菜单 icon 颜色统一
  （svg 填充统一纯 #252632 无透明度，选中深/未选中 45% 灰）、TopNav/页签 nowrap、
  首页自适应断点。唯一无法恢复：用户放的 public/icons/直播.svg（用户素材，浏览器
  缓存无副本）——直播管理菜单暂回退 lucide Video 图标，待用户重新提供。

## 2026-07-03

- 左侧栏菜单文案四字化（除首页）：内容→内容管理、收入→收入变现、服务→创作服务
  （直播管理/数据中心已四字，首页保持）。并统一子页四周间距：去掉数据中心页多余的
  卡外裸标题「数据中心」——它使第一个卡片比其他页低一截；去掉后各子页第一个卡片
  top/left 完全对齐（实测 72/208 一致，标题改由各卡自带）。(未提交)
- 新增「数据中心」菜单 + 首页改版（Figma 904-67124）：左侧栏首项「数据」改名
  「首页」（home 图标），新增「数据中心」项（在内容下方/收入上方）。原首页底部的
  数据总览（雷达）/作品数据/粉丝数据三块整体移入数据中心；新首页底部换成「数据概览」
  板块——最新作品卡（封面+时长+标题+播放/点赞）、账号总览/直播数据 tab + 播放量
  面积趋势图 + 8 个概览指标（较前7日增减，涨红跌绿），右上「查看详情」跳数据中心。
  数据走新端点 `/api/creator/home-overview`（server 聚合近7 vs 前7日差值，三运行
  环境+Vercel 转发）；api.ts 增 useHomeOverview，HomeCharts 抽通用 SimpleAreaChart。(未提交)
- 创作服务三个子页 + 直播管理页 + 头像权限开关：
  · 活动管理（Figma 904-65050）`ActivityPage`：活动灵感招募卡（分页）+ 我的活动表格
    （待开始/审批中/进行中/被驳回/已结束多状态、虚拟/实物奖励、搜索）。
  · 原创保护（904-65582）`CopyrightPage`：蓝色横幅+原创度三步进度、原创权益 7 图标、
    视频讲解（分页）、原创消息公告。
  · 抖音指数（904-64366）`DouyinIndexPage`：顶部页签、同心圆背景+关键词气泡、
    关键词/达人/… tab+搜索、我的订阅空态、实时/飙升双热点榜（前三奖杯+升降箭头）。
  · 直播管理（904-69330）`LivePage`：权限菜单，由顶栏头像里的开关控制，开启时插在
    「内容」上方；封面+时长、火花线+四指标、直播回放/高光/下载/详情/删除操作。
  头像改为 radix Popover（账号信息+「直播管理」iOS 开关，live-store 内存态；关闭时
  若停在该页回落数据看板）。数据走 4 个新端点 activities/copyright/index-hot/lives
  （server 确定性生成，三运行环境+4 个 Vercel 转发接线）；api.ts 抽 makeResourceHook
  通用无参 GET hook。icons 补 Trophy/Gavel/ArrowsLeftRight。(未提交)
- 作品共创页（Figma 904-68923，创作服务→作品共创子项）：新增 `CollabPage.tsx`——
  蓝色渐变横幅（CO 标记+共同创作公约）、剩余次数/常见问题分页卡、共创作品列表
  （封面共创角标+时长、标题、日期、共创人头像叠加组、状态文案），四种状态：正常/
  关系已解除（灰）/整体被平台解除（橙警告条）/部分共创人被解除。数据走新端点
  `/api/creator/collab`（server 确定性生成 6 条，三运行环境接线+api/creator/collab.js）；
  api.ts 增 useCreatorCollab；侧栏 service:作品共创 路由到该页。(未提交)
- AI分身落地页（Figma 891-22823）：新增 `AvatarLandingPage.tsx` 替换占位页——
  ASCII 世界地图纹理打底 + CSS 模糊光斑 + 3D 分身主视觉（素材白底烘死在像素里，
  用 mix-blend-multiply 让白色视同透明）、标题「创所未见 · AI分身」、Ailee 账号
  开通卡（去开通→toast）、AI 聊天/互动空间两张场景卡。5 张素材从 Figma 截取到
  public/icons/creator-center/（hero 初次误截模糊装饰层，改取 891:22894 头像
  节点）。注：preview 截图通道中途故障，视觉部分以计算样式+canvas 像素采样验证。(未提交)
- 首页左侧栏图标换用 public/icons 新素材：发布.svg（黑底按钮内反白）、数据.svg、
  icon.svg（内容）、收入.svg、创作服务.svg；MaskIcon 从 TopNav 抽为独立共享组件
  （CSS mask + currentColor），SIDE_MENU 配置改为 icon URL，删除 lucide 的
  TrendingUp/Coins/Zap 引用。(未提交)
- AI 工坊侧栏删除「运营数据」tab（数据看板已由创作者中心首页承载）：nav 数组
  移除条目，PlatformSidebar 的 onOpenDataOps 属性清理；发布 drawer 里的运营
  数据入口保留。(未提交)
- 载入动画从整页下沉到内容区：外壳（CreatorCenterShell）不再包 motion 整页
  淡入，改为 CreatorCenterHome 内容区（SideNav 右侧）按 page 键控淡入——切
  数据/内容/收入时左侧栏与顶栏完全静止，只有 main 内容动。实测切换动画中
  侧栏/头像漂移均为 0px，内容区透明度过渡可见。(未提交)
- 修切 tab 时顶栏右侧头像晃动：切页淡入动画（y 8→0）会让内容瞬时溢出视口
  底部、文档滚动条闪现→视口宽度抖动→右对齐头像左右跳。外壳内容区加
  overflow-hidden 裁掉瞬时溢出。实测两段切换动画 117 帧采样头像漂移 0px。(未提交)
- 再修顶栏切 tab 晃动：胶囊动画由欠阻尼 spring（会过冲回弹）改为 250ms tween
  ease-out；borderRadius 移入 style 让 framer 在缩放插值时实时校正圆角变形。
  实测动画全程 48 帧采样，按钮最大漂移 0.03px。(未提交)
- AI 工坊侧栏融入大产品：删掉顶部 logo 遗留空块与「收起侧栏」按钮（侧栏常驻，
  PlatformSidebar 移除 onCollapse/themeMode/onChangeThemeMode 死属性）；删掉
  底部账号区（张俊/通知/客服/外观设置，账号统一走创作者中心顶栏）；侧栏默认
  宽度 232→176px，与创作者中心首页/内容/收入页左栏一致。(未提交)
- 修顶栏切 tab 整体晃动：激活态字重从 normal→medium 的宽度变化会让绝对居中的
  菜单条整体重排，全部 tab 统一 font-medium 后按钮宽度恒定（实测切换前后
  各 tab x 坐标位移为 0）。(未提交)
- 顶部 tab 切换过渡动画：激活胶囊用 framer-motion 共享 layoutId 在按钮间
  弹性滑动（spring 420/34），文字/图标颜色 300ms 过渡；非工坊页面切换时
  内容轻微淡入上移（工坊 keep-alive 用 display 切换不参与动画）。(未提交)
- 顶栏换用 public/icons 新 SVG 素材：logo.svg 整体替换音符+文字组合；五个产品
  菜单图标换为 ic-nav-Home/分身/book-open-02/Creation/terminal-square（TopNav
  以 CSS mask + currentColor 着色，激活态自动反白）；右上角由「通知铃铛+角标」
  改为「星光余额」（AI.svg 四角星 + 数量，创作激励计量单位，STARLIGHT=276）。
  另：项目目录由 vibecoding-editor-main-jf 改名为 CreativeStudio。(未提交)
- 创作者中心「收入」页（Figma 904-81576）：新增 `IncomePage.tsx`——我的变现
  （昨日/近7日/近30日/可提现四个金额页签，点击真实切档；趋势图 tooltip 按
  星图任务/小程序推广/音乐推广三来源拆分；昨日档按小时 24 点）、商单任务
  进行中（精选商单行）、变现广场（页签+高收益/保底收入/合作过筛选+搜索均
  可用，卡片为静态营销配置）。数据走新端点 `/api/creator/income`
  （server/creator-data.mjs 从播放表按 RPM 推导收入并按来源确定性拆分，
  三运行环境接线 + api/creator/income.js）。api.ts 增 useCreatorIncome +
  fmtYuan。(未提交)
- 内容管理页「所有时间」改为真实时间筛选：FilterSelect 支持图标态，预设
  所有时间/近7天/近30天/近90天，按作品 publishedAt 与当日回推的下限过滤。(未提交)
- AI 工坊侧栏适配外壳：移除左上角「抖音AI工坊」品牌 logo（与创作者中心顶栏
  重复），「+ AI 创作」按钮从黑底改为白底黑字 + 描边（保留彩虹 hover 光晕）。(未提交)
- 创作者中心「内容管理」页（Figma 904-96111）：作品行提取为独立组件
  `WorkRow.tsx`（封面+置顶/张数/时长角标、标题+行内操作、活动标签、日期状态、
  近14天火花线+指标条——图文/视频指标集不同、流量升降通知条），`ContentPage.tsx`
  铺列表+工具栏（作品/合集页签、体裁/发布状态筛选、搜索均可用；置顶切换/删除为
  内存态演示）。数据来自新端点 `/api/creator/works`（works 表推导累计指标+火花线；
  通知条按「近7日实际衰减 ÷ exp 预期衰减」真实判定，修正了朴素环比人人流量减少的
  问题）；左侧栏状态提升，数据/内容页可切换，其余菜单为建设中占位。修复 spark
  数组误 reverse 导致火花线时间轴翻转的 bug；icons 补 Lock 导出。(未提交)
- 数据总览雷达图五个指标浮签可点击：选中态跟随点击，右侧切换为该指标的
  「{指标}分析」（数值/同类中位数/百分位由接口按维度返回）+「{指标}贡献TOP3」
  （播放量/涨粉按播放贡献、完播率/互动率按作品率值排序、作品数列出本期发布，
  server/creator-data.mjs 为每个维度生成 topWorks + peerMedian + valueKind）。(未提交)
- 创作者中心首页数据改为 mock 后端驱动：新增 `server/creator-data.mjs`
  （确定性生成的 daily_stats 60 天表 + works 作品表 + peer_benchmarks 同类基准表，
  按日期字符串做种子，重启/多环境数据一致），暴露 `GET /api/creator/stats?range=
  yesterday|week|month`——聚合、正态 CDF 百分位（「超过 xx% 同类作者」）、渠道拆分、
  逐日 Top3、昨天档按小时拆 24 点都在服务端算；dev(Vite 中间件)/prod(Express)/
  Vercel(api/creator/stats.js) 三处接线。前端删掉全部硬编码数字：新增 api.ts
  （类型 + useCreatorStats hook，含缓存 + 并发去重），三个数据面板独立 range
  真实重查、指标页签切真实序列、雷达图/分析文案/TOP3/资料头数字全部来自接口，
  加载骨架 + 错误占位。(未提交)
- 产品升级为「抖音创作者中心」外壳：新增 `src/modules/creator-center/`
  （TopNav 顶部产品菜单 + CreatorCenterHome 数据首页 + PlaceholderPage 占位页 +
  CreatorCenterShell 路由外壳），按 Figma「抖音AI创作工具盘点/数据」稿实现——
  资料头、智能创作/作品发布入口卡、数据总览（recharts 雷达图 + 指标浮签）、
  作品数据/粉丝数据（面积趋势图 + 富 tooltip）。现有抖音 AI 工坊挂在顶栏
  「AI工坊」入口下（fixed 根元素/侧栏改读 `--cc-top` 让位 48px 顶栏，首次进入后
  keep-alive 保持工作状态）；AI分身/百科/随变 为「即将上线」占位页。新增
  recharts 依赖、`Coins` 图标导出、入口卡图取自 Figma（public/icons/creator-center/，
  均 <15KB）。顺带 compress-images 全量跑了一遍，27 张存量图压小 631K。(未提交)
- dev server 支持端口自动切换：vite.config.ts 读取 `PORT` 环境变量，launch.json 开启
  `autoPort`（5173 被其他会话占用时预览能自动换端口启动）。(未提交)

## 2026-05-22

- H5 编辑面板改成可拖动浮层（默认右侧 → 跟随选中对象）：新建 `H5FloatingEditPanel.tsx`
  （`position:fixed` 卡片 z-50，宽 300、高 min(72vh,540)；useLayoutEffect 在 selection
  变化时定位——有 `data-h5-active` 节点就放到它右侧+16px 间隙、否则默认右侧 24px；
  header pointerdown 起拖、window 监听 move/up，全程 clamp 进视口；窗口 resize 也 clamp）。
  `H5LayerEditPanel` 加 `floating` + `onHeaderPointerDown`：header 变 cursor-move 拖拽手柄
  并加 Move 图标，关闭按钮 stopPropagation 防误拖。`MarketingH5Preview` 给当前选中的
  El/Selectable 打 `data-h5-active` 供浮层定位。VibeCodingPage 把 marketing-h5 从右侧
  停靠列排除、改渲染浮层（预览因此满宽）。实测：默认右侧→点主标题/抽奖按钮浮层跟到
  元素旁(gap16)、拖 header 精确位移(-300=±300)、拖后再选新对象会重新跟随。tsc 通过。(未提交)

- H5 编辑改两级选择（楼层 + 楼层内元素，原来只能选整楼层颗粒度太粗）：
  `H5LayerEditPanel` 把选择从 `H5LayerId | null` 升级为 `H5Selection`（layer /
  element 两态，null=整体配置），新增元素级编辑器——文本(文案·字号·文字颜色·
  对齐·加粗) / 按钮(文案·底色·文字颜色·圆角) / 图片(缩略图 + 上传·再次生成·画布
  编辑 + 适配方式)，header 面包屑「楼层 / 元素」、footer 跟着变。`MarketingH5Preview`
  把原来全覆盖 overlay 的 `Selectable` 改成不挡内部点击的楼层环（楼层选中=实线环+
  四角手柄+标签，楼层内有元素选中=淡环），并加 `El` 元素拾取器包住各楼层关键原子
  （头图图片/主标题、介绍标题/正文、抽奖标题/名单/抽奖机图/抽奖按钮/我的奖品按钮、
  任务标题/任务名/CTA、规则标题/正文），非编辑态 `El` 原样透传保证像素不变。
  VibeCodingPage 状态 `h5SelectedLayer→h5Selected` 并改 props（selected/onSelect、
  selection）。实测：点主标题→元素文本编辑器(预填文案+字号20+对齐居中)、点抽奖按钮→
  按钮编辑器(预填「立即抽奖」)、点头图→图片编辑器(缩略图+三动作+适配)、点倒计时空白
  →楼层编辑器、点背景→整体配置；tsc 通过。(未提交)

- AvatarPicker 加 `showUrlInput` 开关：右侧「编辑」面板隐藏「图片地址」输入行
  （传 `showUrlInput={false}`，无 URL 行时按钮与缩略图垂直居中），只留上传/生成；
  「基础信息」表单仍保留地址行。(未提交)

- 分身头像支持本地上传 + AI 生成：新增共用组件 `AvatarPicker.tsx`（缩略图 +
  图片地址输入 + 「本地上传」+「AI 生成」），接入 `ProductEditPanel`（编辑面板）
  与 `AvatarBasicInfoForm`（基础信息表单）两处。本地上传走真实 `<input type=file>`
  → FileReader 读成 data URL；Kimi 无文生图能力（仅 Chat + Vision 输入），故 AI 生成
  改用免费头像服务 DiceBear（adventurer 风格）按「分身名称+描述」当 seed 出图，
  自增计数让每次点击换一张。实测：AI 生成 seed 0→1→2 各出一张并成功加载、本地上传
  注入 PNG 即时渲染 data URL。(未提交)

- 从 0 生成分身（端到端）+ 运行时配置 store：新增 `artifact/runtime-config-store.ts`
  （zustand，按项目名存放 AI 生成的配置，覆盖静态 mock；默认空 ⇒ 5 个 demo 仍走静态）
  与 `artifact/generate.ts`（`generateAvatarConfig` 用 Kimi 流式产出 JSON 配置，
  extractJsonObject 去 ``` 围栏 + 取最外层 {}，asString/asStringArray 归一化，缺字段
  回退 DEFAULT_AVATAR_PREVIEW，输出完整 AvatarAppConfig 含 preview 块）。VibeCodingPage
  订阅 runtimeConfigs，分身/小程序/H5 三处渲染改成 `runtimeConfigs[projectTitle] ?? 静态`；
  submitFromHome 在 kind==='ai-avatar' 时调 generateAvatarConfig→setRuntimeConfig，
  若仍在该项目则打开预览。补 projectTitleRef 解决闭包陈旧。实测：首页输入「会聊星座
  情感的分身·小星」→ 右侧实时渲染生成的「小星情感师」(bio/欢迎语均来自 Kimi)，
  且陶白白等 demo 仍渲染各自静态配置、无串味、像素不变。(未提交)

## 2026-05-21

- 配置驱动产物（第三刀·H5）：新建 `MarketingH5ConfigData.ts`(MarketingH5PreviewConfig +
  DEFAULT_MARKETING_H5_PREVIEW + 按项目名注册表 + getMarketingH5Preview)；
  MarketingH5Preview 各区块(头图/倒计时/介绍/抽奖/任务/规则/footer)改成读 preview
  渲染，布局/装饰/图层选择仍是框架；六一活动配置=默认值，渲染处传 preview。
  实测六一 H5 与改前像素一致、无报错。至此 内容型(分身/小程序/H5)均配置驱动，
  网站/游戏归 embed 不动，提案本就数据驱动。
- 配置驱动产物（第二刀·小程序）：MiniAppPreview 4 个页面（首页/塔罗/聊天/个人）
  改成读 `MiniProgramConfig.preview` 渲染——新增 `MiniProgramPreviewConfig` +
  `DEFAULT_MINIPROGRAM_PREVIEW`(场景图/头像/卡池/导航标题/首页/塔罗文案/聊天/个人)；
  塔罗小程序配置带上 preview=默认值；渲染处传 config。金色主题 + 翻牌机制 + 布局
  仍留在渲染器作框架。内容(文本/图/数据)走 config，便于按场景拆模块各自维护。
  实测塔罗 4 屏与改前像素一致、无报错。
- 配置驱动产物（第一刀·分身）：把 AI 分身预览从写死改成读配置——AvatarConfig 新增
  `preview`(displayName/avatar/bio/timestamp/seed对话/评论thread) + `DEFAULT_AVATAR_PREVIEW`
  兜底；`AiPersonaChatPreview` 接 `config` 渲染（私信+评论两屏全部读 config，无 config
  时回退默认）；陶白白配置带上 preview=默认值。VibeCodingPage 渲染处传
  `config={getAvatarConfig(projectTitle)}`。目标：框架真、配置当 mock，换 config=换产物。
  实测陶白白两屏与改前像素一致、无报错。(未提交)
- 首页进入对话流时右侧预览默认不打开（Artifacts 语义）：home 创建的项目延迟打开
  右侧（initProjectDefaults 加 deferProduct → openTabs=[]），仅在产生可预览产物时
  打开——任何后续 sendChat（追问/点修改建议）或脚本流程产出（formSubmitted /
  触发器确认 / 提案 / 游戏已有的延迟）触发 seedProductTabs。初始 home prompt 用
  `sendChat(text,{fromHomeEntry:true})` 排除。侧边栏打开的既有项目不受影响、照常
  立即显示。实测：提交后右侧关、首次交互后打开、既有项目立即打开。(未提交)
- VibeCodingPage 瘦身（god 组件拆分 阶段0 切片）：抽出 `proposal-docs.ts`(7 个提案
  markdown 生成器 + PROPOSAL_FILE_SUMMARY)、`FigmaIcon.tsx`(共享图标)、
  `PlatformHome.tsx`(PlatformHome + SceneGlyph + HOME_SCENES + 类型)；清理随之
  失效的 import(PROPOSAL_* / Bot)。VibeCodingPage 9914→9231 行(−683)。tsc 通过、
  预览实测首页/场景 flyout/提案文档均正常。PlatformSidebar(640 行,prop 较重)留作
  下一步。(未提交)
- 工程优化（建议 1-4）：
  · 健壮性：新增 `shared/components/ErrorBoundary`，包住 App 根 + 右侧预览
    (resetKey 跟 项目/tab 走)，单个预览崩溃不再白屏整个壳。
  · 交互反馈：`ToolbarAction` 无 onClick 时默认弹 toast「「X」功能演示中…」，
    避免 demo 动作点了没反应。
  · 性能：给重的预览/页面组件加 `memo`(AgentHubPreview / GarudaGamePreview /
    MiniAppPreview / AiPersonaChatPreview / ResourceHub / DataOpsView),顶层
    状态(缩放/控制台等)变化时这些子树不再全量重渲染(props 均为稳定值)。
  · 缩放拖拽：确认缩放已限定在 previewSurface(预览 tab),画布编辑/分隔条/控制台
    等坐标型拖拽都在缩放区域外,预览本体内无坐标型拖拽 → 无需改动。
  预览实测:toolbar 动作弹 toast、资源库/运营数据正常渲染、无报错。(未提交)
- 分身「技能」工具栏加上「跳转」操作(原来只有知识库有):技能/知识库分支统一渲染
  添加 + jumpToSourceAction;跳转 toast 文案泛化为「正在跳转到来源…」以适配技能。
  预览实测技能 tab 出现 添加 + 跳转。(未提交)
- 预览缩放居中修复 + 按钮手型:① 缩放改为以视口中心为锚——给预览画布加 ref,
  previewZoom 变化时把 scrollLeft/Top 居中(溢出时 m-auto 会退化成左对齐导致内容
  偏移)。② 修复 Tailwind v4 preflight 去掉的 button 手型:index.css 加全局规则,
  让 enabled 的 button/[role=button]/label[for]/summary 显示 cursor:pointer;并把
  该约定写进 CLAUDE.md。(未提交)
- 预览缩放范围收窄:只缩放「手机预览/产物画布」本体,不再缩放上方 toolbar 和点阵
  背景。把缩放容器从「整个 IIFE 外层」移到 phoneView 里只包 previewSurface
  (画布区改 overflow-auto + sizer m-auto 居中);缩放控件只在「预览」tab 显示。
  实测 toolbar 尺寸不变、手机随缩放变化并居中。(未提交)
- 预览缩放微调:① 改为以画布中央为锚点(缩放视口 flex + sizer m-auto,缩小居中、
  放大从中间扩展且可滚动);② 缩放控件与左侧 terminal 切换的阴影减轻
  (0_6px_18px/0.5 → 0_2px_8px/0.1)、底色统一改成白色 + 极淡边框。(未提交)
- 首页输入框 placeholder 改为「请描述你的需求，我来帮你完成～」。(未提交)
- 右侧预览右下角新增缩放控件(− / 百分比 / +,点百分比重置):整体按比例缩放
  预览内容,50%–200%、步进 10%。实现用「百分比 sizer + transform scale」——内容
  按自然尺寸渲染再整体缩放,>100% 可滚动、<100% 在面板内缩到左上;外层标签/发布
  等 chrome 不缩放。新增 previewZoom 状态、icons 补 Minus。预览实测 100/120/60%。(未提交)
- 分身代码文件里的 persona.yaml(陶白白 Sensei)从 28 行简版扩成 80 行丰富版:
  新增 meta / identity(role·background·audience·goal·greeting) / persona
  (tone·style·pace·traits·values) / voice(口头禅·emoji·禁用词) / principles /
  examples(few-shot) / guardrails / knowledge_refs·skill_refs。预览实测代码视图
  正常渲染高亮。(未提交)
- 游戏「玩法」对象加回到标签页的「+ 添加」菜单(不放回左侧产品列表):gameView
  仍隐藏 玩法,但 VibeCodingPage 的 + 菜单对 web-game 额外插入「玩法」行(排在
  代码文件前)。点开即打开 玩法 标签页(ProjectObjectViews 已有的核心循环/武器
  系统/敌人波次/Roguelike/Boss/道具掉落卡片视图)。预览实测 + 菜单出现玩法、左侧
  无玩法、点开渲染正常。(未提交)
- 修复游戏画布编辑(ImageCanvasEditor)选中图片的浮动工具条不跟随的问题:原来
  工具条固定在画布顶部居中(`left-1/2 top-3`)。改为按选中图片定位——浮在图片
  上方(空间不足时翻到下方)、随拖动与画布滚动跟随,并在视口内夹取(条比画布宽
  时居中)。新增 scroll 状态 + onScroll,工具条用 useLayoutEffect 量自身宽度做
  夹取。预览实测拖动时工具条跟随。(未提交)
- 资源库「知识库」按设计稿落地图片卡:照片 banner + 居中 48px 圆角图标浮层 +
  标题/数量(同行)/描述。从标准知识库取 8 张(数据 6 + 结构化 2),照片缩到
  584px、图标缩到 96px 存 `public/assets/resource-hub/knowledge/`;KnowledgeBase
  数据加 `image`/`icon` 字段,KNOWLEDGE_FILTERS 裁剪为 全部/数据知识库/结构化
  知识库;cards.tsx 新增 `KnowledgeBanner`、抽出复用的 `UsageCount`,删除弃用的
  `CountPill`。预览实测卡片渲染与分类筛选正常。(未提交)
- 统一右侧面板底色为 `--color-surface-0`(#fafbfc):创意广场 AgentHubPreview
  去掉白→#f8f9fb 渐变 + 创意广场 wrapper `bg-white`→surface-0;Skills
  ResourceLibraryView 根容器与两处 sticky 头由 `bg-white`→surface-0(资源库/
  运营数据本就是 surface-0)。
- 右侧面板加载入动效(参考创意广场的淡入上浮):ResourceHub body 按 tab
  keyed `motion.div` 淡入上浮(切 tab 也重播);Skills/运营数据在
  VibeCodingPage 的 wrapper 包成 `motion.div` mount 淡入上浮(创意广场本就有
  分段 stagger)。预览实测四个面板底色一致、载入有动效。(未提交)
- 资源库「工具箱」卡片改为设计稿的图片版:卡片顶部用真实预览图 banner
  (替换原渐变+气泡 mock),底部 footer 改为 抖音icon+来源｜日期｜使用量。
  从设计稿取 8 个真实工具(覆盖 6 个分类),预览图下载并用 tinify 缩放到
  584px 宽 + 压缩(5–24KB)存到 `public/assets/resource-hub/tools/`;TOOLS
  数据精简为这 8 条并加 `image` 字段,TOOL_CATEGORIES 裁剪到现有分类;
  cards.tsx 新增 `ImageBanner`/`ToolFooter`,删除弃用的 `BubbleBanner`/
  `MetaFooter`。资源库预览实测正常。(未提交)
- AgentHubPreview 在「精品推荐」和「发现更多」之间新增「业务产品」模块:从 Paper
  设计稿落地 11 张产品卡(图标/标题/标签/描述),图标下载到
  `public/assets/agent-hub/business/`(7 个唯一 webp,均 ≤2KB);新增
  `BUSINESS_IMG` 常量、`businessProducts` 数据与 `BusinessProductCard` 组件,
  网格 `grid-cols-1 @[560px]:grid-cols-2 @[900px]:grid-cols-3`。创意广场预览实测
  正常渲染。(未提交)
- AgentHubPreview 精品推荐从一排 3 个改成一排 5 个:`slice(0,3)`→`slice(0,5)`、
  网格 `@[460px]:grid-cols-3 @[720px]:grid-cols-5`(展示全部 5 个 featured)。
  预览实测创意广场精品推荐一排 5 个。(未提交)
- 创意广场里隐藏 AgentHubPreview 自带的二级左侧导航:`AgentHubPreview` 新增
  `hideSidebar` prop(默认 false,web-app 项目预览不受影响),为 true 时不渲染左侧
  `<aside>` 栏;创意广场处传 `<AgentHubPreview hideSidebar />`。tsc 通过,预览实测
  创意广场内容铺满、无二级侧栏,项目预览侧栏仍正常。(未提交)
- 创意广场菜单直接复用「抖音 AI 工坊设计探索」页面:`platformCreativeSquareOpen`
  从 `PlatformPlaceholderView` 占位改成渲染 `<AgentHubPreview />`(同 web-app 项目
  预览,容器加 `@container bg-white`)。`PlatformPlaceholderView` 已无引用,删掉
  其 import。tsc 通过,预览实测创意广场显示智能体广场(Hero/精品推荐/发现更多)。(未提交)
- Skills 菜单页精简:页面标题从「资源库」改成「Skills」,去掉顶部 type tabs
  (Skills/工具、知识库、模型、发布器)整行——Skills 下不再出现知识库/模型/发布器。
  `ResourceLibraryView` 内删除 `TYPE_TABS` 渲染+常量、`onTypeFilterChange` prop、
  `CAPABILITY_LABEL` import;父组件 `typeFilter` 直接传 `"skill-tool"` 字面量,
  删掉 `resourceLibraryTypeFilter` state 与 `ResourceLibraryTypeFilter` import。
  tsc 通过,预览实测标题为 Skills、无 type tabs、来源树/卡片正常。(未提交)
- Skills 菜单改回「原封不动」搬原资源-Skills 页:左侧 Skills 菜单不再渲染脑补的
  `SkillsHub`,而是直接挂回原 `ResourceLibraryView`(默认 `typeFilter='skill-tool'`,
  保留来源树/内场外场/真实 RESOURCES 卡片)。恢复被删的 `resourceLibrary*` state、
  `toggleResourceLibraryExpanded`、`useCapabilityInChat`、`ResourceLibraryView`/
  `TypeFilter`/`PrimaryCategory` import;删除不再使用的 `resource-hub/SkillsHub.tsx`。
  资源库菜单仍是新的 5-tab `ResourceHub`。tsc 通过,预览实测 Skills 菜单显示原页、
  资源库显示 5 tab,无报错。(未提交)
- 资源库照图整体重建(抽象组件 + 脑补数据):新建 `resource-hub/`(`data.ts` 脑补
  工具/知识库/模型/发布器/触发器/Skills 数据;`cards.tsx` 可复用卡片原语:Chips/
  FilterGroup/SectionTitle/CardShell/各类 Banner/CardBody/MetaFooter;`ResourceHub.tsx`
  5 tab 页;`SkillsHub.tsx` Skills 菜单页)。资源库改为 工具箱/知识库/模型库/发布器/
  触发器 5 tab;Skills 从资源库移到左侧 Skills 菜单。替换并清理了旧 ResourceLibraryView
  的渲染 + 相关 state(resourceLibrary*/useCapabilityInChat 等)。tsc 通过,预览实测
  5 tab 内容、Skills 菜单、即将上线徽标均正常,无报错。(未提交)
- 分身项目隐藏「人设」对象(产物视图 + 默认 tab 都去掉),把人设落成真实文件
  `avatar-agent/persona.yaml`(加进 aiPersonaFileTree + codeFiles,YAML 含
  `# 人设`/`# 知识库`/`# 技能` 映射注释),在「代码文件」里可查看。tsc 通过,
  预览实测:人设 tab/对象消失、代码文件显示 persona.yaml 及注释。(未提交)
  ⚠ 审计发现:各项目「代码文件」用的是同一份按文件名匹配的全局 codeFiles,导致
  web-app(设计探索)的 index.tsx/api.ts 错配成了小程序(Taro)代码;avatar 除
  persona.yaml 外其余配置文件无代码内容。待后续按项目重构 codeFiles。
- 侧栏顶部按钮「新建项目」改为「AI 创作」并左对齐(icon size16 + px-2,与下方
  Skills/资源库等菜单图标对齐,实测都在 left:20);Tooltip 高度调为 24px(`h-6`,黑底)。
  tsc 通过,预览实测对齐 + tooltip 24px 黑色。(未提交)
- 项目列表头部:新增「查看全部项目」icon button(folders 图标,点击展开全部项目);
  三个图标加大(`h-5 w-5`/size12 → `h-6 w-6`/size15);原生 `title` 全部改用新建的
  黑色 Tooltip 组件 `Tooltip.tsx`(基于 `@radix-ui/react-tooltip`,黑底白字+箭头、
  portal 不裁切)。tsc 通过,预览实测:3 按钮无原生 title、展开全部生效、hover 出
  role=tooltip。(未提交)
- 左侧 chat 对话流内容左右 padding 加大:消息容器 `px-2.5`→`px-5`、输入框
  `mx-2.5`→`mx-5`(保持对齐),内容不再贴边。实测左右各 20px。(未提交)
- 运营数据页「已发布项目」左侧图标改用项目对应图片(复用发布抽屉的
  `getPublishObjectVisual`/`PublishObjectVisualThumb`,从 PublishDrawer 导出),
  与发布下拉保持一致;原来是按 kind 的通用 lucide 图标。tsc 通过,预览实测各 kind
  图标正确(分身/小程序/H5/游戏=图片,web-app=`S°`角标)。(未提交)
- 首页 slogan 改为「所见即所得，链接抖音生态」(原「所见即所得，一站式满足需求」)。(未提交)
- Radix Popover 样例:把侧栏底部「设置(外观)」浮层改成 `@radix-ui/react-popover`
  (portal 到 body → 不再被侧栏 overflow 截断;side=top align=end 往左上弹出 +
  collisionPadding 防溢出;`Popover.Close` 选完即关;沿用现有 token 样式)。删掉了
  原来的手写 `layoutMenuOpen`/`layoutMenuRef`/outside-click effect。tsc + vite build
  通过,预览实测:内容完整不截断、在视口内、切主题/关闭正常。 — `349e9b4`
- 设置弹层精简:两处设置/更多弹层(侧栏底部齿轮 + 顶部「更多」)去掉「布局」整段,
  只保留「外观」(亮/暗)。底部齿轮弹层从 `right-0` 改 `left-0`+缩窄,修复超出屏幕
  (实测无四向溢出)。顺带清理因此空出的 `setLayout`/`onChangeLayout`/`layout` props
  与 `Code2/Columns2/LayoutDashboard` 图标 import;layout 暂固定为 platform。
  tsc 通过,预览实测弹层只剩外观、在视口内。 — `349e9b4`
- 侧栏项目展开/收起加动画:用 framer-motion `AnimatePresence` + `motion.div`
  (height 0↔auto + opacity, overflow-hidden)包裹项目子对象树,展开收起平滑过渡。
  tsc 通过,预览实测展开/收起正常、有动画容器、无报错。 — `349e9b4`
- 侧栏置顶项目:把左侧 folder 图标替换成 pin 图标(`Pin`/`ti ti-pin`),并去掉
  原来跟在项目名后面的那个 pin。未置顶项目仍显示 folder。tsc 通过,预览实测置顶项
  左侧为 pin、未置顶为 folder、名称后无 pin。 — `349e9b4`
- 首页输入指令改为「新建项目」逻辑:`submitFromHome` 不再把 prompt 丢进现有
  小程序,而是按 prompt 分类(`classifyProjectKind`)生成一个全新项目(名字取自
  prompt),登记进侧栏(`createdProjects` + 新增 `createdProjectKinds` 动态 kind
  映射 + `kindOf()` 统一查询,替换主组件内所有 `PROJECT_KINDS[...]` 读取),切到
  该空项目并把 prompt 送入其对话(走需求收集等新建流程)。tsc 通过,预览实测:
  首页提交 → 侧栏出现新项目、面包屑切到新项目、右侧空态、需求流启动,无报错。 — `349e9b4`
- 重构 Step 2(抽离无状态组件,纯移动):抽出 `Toolbar.tsx`(`FlexAlignGlyph` /
  `ProductToolbar` / `ToolbarAction`)、`FileTreeView.tsx`(`getFileIcon` + `FileTreeView`),
  并清掉主文件因此空出的图标 import。主文件 10,079 → 9,820 行。tsc 通过,预览实测
  toolbar(知识库 跳转/添加)、侧栏树、代码视图均正常,无报错。 — `349e9b4`
- 重构 Step 1(瘦身 `VibeCodingPage.tsx`,纯数据外移、零逻辑改动):抽出
  `data/project-docs.ts`(项目文档 MD + `PROJECT_DOCS`)、`data/chat-suggestions.ts`
  (`GENERIC_AI_REPLIES` / 各 `CHAT_SUGGESTIONS_*`)、`data/project-kinds.ts`
  (`OutputShape` + `PROJECT_KINDS` / `SHAPE_BY_KIND` / `PROJECT_KIND_LABELS`)。
  主文件 10,497 → 10,079 行。tsc 通过,预览实测项目可正常打开、无报错。 — `349e9b4`
- 用 TinyPNG 批量压缩 `public/bg`、`public/assets` 下 59 张在用图片(头像≤512px、其余≤1600px,
  原地覆盖、文件名不变):**77MB → 12.6MB,省约 64.8MB**;`public` 295M→232M。
  新增 `scripts/compress-images.mjs`、`tinify` devDep、`.env` 的 `TINYPNG_API_KEY`。
  并把「图片必须压缩、保持小体积」写进 CLAUDE.md 规则。 — `349e9b4`
- 新增 `WORKLOG.md`(本文件)— 用于持续记录每次改动。 — `349e9b4`
- 新增 `CLAUDE.md` / `README.md` — 项目导航(给 AI)与项目介绍(给人)。 — `349e9b4`
- 删除 `public/` 下 62 个未引用的孤儿资源(约 51.5M,含 `models/character.glb` 19M、
  `bg/` 一批概念图/视频、`bg/presets|library|voice/` 等);保留动态引用的 `flex-align-*.svg`。 — `349e9b4`
- 对话内输入「我要发布」改为开关式「选择发布场景」卡片(对齐发布弹窗样式);
  `知识库 / 数据库`(三方引用数据)toolbar 新增「跳转」入口。 — `1e8090a`
- 接入真实 Kimi 流式对话:新增服务端代理 `server/kimi.mjs`(共享)、`server/index.mjs`(Express)、
  `api/chat.js` `api/health.js`(Vercel Functions)、`src/shared/api/chat.ts`(SSE 客户端);
  key 仅存服务端(`.env` / 平台环境变量),浏览器只访问同源 `/api/chat`。 — `7507aaf`
- 左侧项目树恢复内联展开「多子对象」(默认展开,3 级带竖线);
  游戏项目去掉 `知识库 / 数据库`;游戏预览去掉「新窗口打开」跳转按钮。 — `7507aaf`
- AgentHub 设计探索:精品推荐由 5 改 3 + 网格上限 3 列;Hero 标题字号调小;
  修复「+」下拉里重复的「代码文件」行。 — `7507aaf`
- 「+」标签下拉过滤掉已经打开为 tab 的对象。 — `040d0e3`
- 首页 ASCII 背景视频画面下移至 95% 取景，并整体放大 5% 做左右过扫描裁切，消除素材两侧暗边，底部柔化遮罩保持不变。(未提交)
- 首页视频底部渐变遮罩同步放大 5%，并整体下移渐变区间，确保过扫描后的画面边缘与遮罩柔化位置一致。(未提交)
- 首页整体按 Figma 节点 20:7226 校准：侧栏 200px、内容 24px 边距、资料区坐标、264px 快捷入口、430px 视频遮罩、400px 数据概览与下方固定高度卡片，并保留 kingjaylee 头像及活动/互动同排布局。(未提交)
- 集成 Marketing King「夏日冲浪 · 顺风顺水」营销 H5：新增独立项目入口、活动方案与产物树，接入原活动夏日 KV/装备卡/奖励/内容素材，新增可演示的抽装备、装备册、档位奖励、任务、主题切换、话题内容和规则/奖品弹窗闭环。 — 未提交
- 收口夏日冲浪项目的旧 H5 编辑入口，避免进入不匹配的春节画布编辑器；构建通过，浏览器交互验证通过。 — 未提交
2026-08-03：将夏日冲浪右侧编辑面板继续对齐 Marketing King 原版：补入夏日/夜食双主题草稿、草稿本地持久化、原版夜食主题数据、素材 assetId 元数据与 M2 过场动画复用库；活动配置刷新后保留，未提交
2026-08-03：对齐原版 M2 默认行为，打开集卡面板即选中首个 Hero 图层，并在草稿切换/复制/导入/删除后同步选择状态，未提交
# 2026-08-05 默认展开玩法一级配置

- 根据运营配置页的可发现性要求，将抽奖、集卡、任务、投票、答题的一级内容与规则面板改为默认展开，单张卡片、奖品、任务等记录详情仍按需展开。
- 风险频控和玩法文案作为低频高级设置保留默认收起；开合按钮由“编辑”改为“展开 / 收起”，折叠时仍保留导入、素材库、模拟等操作入口。
- 删除集卡、任务、投票、答题页的单面板重复分组标题，减少标题和说明的重复层级。
- 2026-08-06：确立并落地“活动主流程内核 / 玩法组件”两层架构；ACG 项目文档新增结构化主流程，玩法配置升级为四个独立可编辑组件实例，资产模板同步补齐 coreFlow 与组件槽位（未提交）。
